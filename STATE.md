# STATE.md — Current Repository State

> The only live status record. Keep it short and current: current status, outstanding limitations, the next action. Not a backlog, roadmap, or branch log. Git history holds prior states of this file; `DECISIONS.md` holds ratified decisions; the git-ignored `.handoffs/` directory holds task files, packets and dispositions as historical evidence, never as current instructions.

**Updated:** 2026-09-22 (Phase 5 complete and portfolio-ready; D-016 merged and verified on `main`; D-017 built on `correction/scoring-successor-v5.2` and **accepted at `f944ccc`** by implementation review; merge is the user's decision and is unauthorized)

## Phase

**Phase 5, Insights, dataset and portfolio gate. Scope ratified as D-014 on 2026-09-14 (decision packet revision 3, two reviewer rounds at `3e96c19`). Delivery: 5A dataset and analytics engine, 5B Insights screen, 5C README and gates. Phase 5A is complete as a build: accepted at `6e1b24cd386aa8f10835fc30ce7c01f37d5886d6` (2026-09-14, `.handoffs/review-disposition-p5a.md` and `.handoffs/review-disposition-p5a-implementation.md`) and merged as `b75da53` on 2026-09-15 on the user's explicit word: "dataset and analytics engine complete". Phase 5B is complete as a build: accepted at `05b93690053c15b8c625abcd2c0289e2ad867bd4` (2026-09-16, `.handoffs/review-disposition-p5b.md`) and merged as `ab42ed7` on 2026-09-16 on the user's explicit word: "merge". **Insights complete; AC-16 closed.** Phase 5C is complete as a build: accepted at `8d18cf57fe9fe1311e000b4bc73cddda54866166` (2026-09-19, `.handoffs/review-disposition-p5c-correction-2.md`) and merged as `e335725` on 2026-09-19 on the user's explicit word: "merge". **Phase 5 is complete and both `PRODUCT.md` §12 gates are met: build-complete and portfolio-ready.** No deployment is authorized; hosting stays parked by D-014.** Phase 4 (outcome attribution, D-013) is complete as a build: 4A accepted at `1050018b9b58f6d4cc3923710467e39284063a8d` and merged as `0e13591` (2026-09-13); 4B accepted at `188bc8912f475133d5490f151df478aaa0a56ce7` (`.handoffs/review-disposition-p4b.md`) and merged as `22c6832` (2026-09-14), both on the user's explicit word; keyboard ruling: automated key input that exercises real browser behavior with focus, activation and state observed is sufficient, no physical-keyboard pass or assistive-technology audit claimed. Phases 0 to 3 are complete and merged (Phase 3: engine `61b58c9`, screen `bce30d9`).

Completion language in force: 4A "attribution engine and persistence complete"; 4B "attribution experience complete, subject to explicit acceptance and invariant mapping"; AC-16 is not closed until Insights implements the decision-metrics exclusion (5B). 5A completes as "dataset and analytics engine complete"; 5B as "Insights complete; AC-16 closed", a wording the reviewer permitted on 2026-09-16 with the engine-to-page exclusion mapped to the accepted commit. No deployment is authorized.

## Present Objective

Phase 5 is complete and the MVP is delivered. No phase is in progress. One bounded correction sits outside the phase sequence and is **built, corrected and accepted, unmerged**: the D-017 scoring correction, ratified by the user on 2026-09-21. `v5.1`'s positive weights sum to 72 against threshold 75, so the ratified current logic can output `PRIORITIZE` for no context at all (coordinator-verified against the shipped evaluator and the shipped generator, twice by independently written probes: 0 of the 240 generated accounts). D-017 corrects this by registering an immutable successor `v5.2` — `v5.1`'s five positive factors unchanged, the sixth factor changed from `verified_integration_pressure equals 'LOW'` at −21 to `verified_integration_pressure equals 'HIGH'` at +15, threshold 75, positive weights totalling 87, canonical content hash `cbefd0508d9c999de299b8ed7a5d60f38b746dfc81520e798bd6c25515e0b889` — and making it the demo default, without editing or retiring `v3.2` or `v5.1`. The original 86 → 51 comparison stays exactly reproducible under its own two artifacts; the default rendered comparison becomes 86 → 72. A small visible warning names the condition when an artifact's positive weights total below its threshold, reported as an upper bound under `evaluator-v1` rather than a proven maximum; it rejects nothing and changes no stored content. Registration is a repeatable collector overlay outside `fixtures/canonical/`, registered after the dataset is complete because the seed schedule requires its prefix sequences intact, so the nine canonical envelopes, their nine pinned hashes, the two-artifact generator pair and the dataset seed schedule are untouched. The weights are synthetic demonstration choices and are described as such, never as business rules validated by synthetic Insights. **Pre-dispatch review is complete.** Task revision 1 (SHA-256 `6891dcdc…e7a0`) and packet `.handoffs/review-packet-d017-predispatch.md` (SHA-256 `1abb5bdf…32fe`) were reviewed against `main` at `6b6da168fb0fdc9ffce8ef1d3a2764c03757429e`; the ChatGPT project reviewer returned **ACCEPT WITH CONDITIONS** on 2026-09-21 (`.handoffs/review-disposition-d017-predispatch.md`, SHA-256 `0d079125…b56e`), with five bounded corrections and "must not be dispatched unchanged". All five are applied in **task revision 2, SHA-256 `c31acdaf0cb9bc038fdc2cd24ebb89d1055a9b9f449a5e30073b4546c3f5c11e`**. The disposition states mechanical incorporation needs no further full pre-dispatch round. The substantive correction is the setup order: `dataset/schedule.py:249` requires every event inside the scheduled prefix to hold its exact sequence, so the overlay must be registered **after** a complete `seed-dataset`, never before it — the reviewer reproduced `ScheduleDiverged` at sequence 1 on an empty database and at sequence 10 after `seed`, with full state unchanged across each refusal. Revision 1's "either order" requirement was impossible and is replaced by a supported order plus a negative test. The other four: scoped maintenance of the existing `v5.1`-default tests is now explicitly authorized rather than forbidden; the warning reports a positive-weight **upper bound** under `evaluator-v1` rather than a proven maximum, because the grammar admits an unsatisfiable rule; the threshold-65 mutation is mapped to the hash and canonical-score assertions instead of a test the reviewer proved cannot detect it; and the placeholder commit and false clean-tree claim are replaced by real anchors. The packet is left at its reviewed bytes as historical evidence and is not edited; this record is the reconciliation. **D-017 is built and under implementation review; merge and deployment remain unauthorized.** Revision 2 was dispatched on 2026-09-21 and built on `correction/scoring-successor-v5.2` at `a832c55aad1c99ef1b0be08fbdf62d9dbe924c14` from base `fd5db98ff0cc7d7491d6acddba54b54bce391e70` (three commits, 18 files, 1,438 insertions, 61 deletions). The builder reported a scope conflict rather than softening it: making `v5.2` the default broke six existing tests in two modules the task and the dispatch supplement protect. Coordinator verification at that commit reproduced the report — 690 passed and six failed, five of them the CI-profile invariants; ruff clean at 91 files; the pinned `v5.2` hash `cbefd0508d9c…0b889` exact; nine of nine canonical event hashes unchanged; five of the seven required mutations independently re-killed by their named assertions. Packet `.handoffs/review-packet-d017-implementation.md`, SHA-256 `0a23de744bd935f28b37699850476261219e44fcc80a4f1bb6e4a5b43d7702e7`. The ChatGPT project reviewer returned **CHANGES REQUIRED** on 2026-09-21 (`.handoffs/review-disposition-d017-implementation.md`): the four-line fixture exception is **approved** as a narrow extension of the task's §7 and a narrow lift of the supplement's invariant-module edit protection, and four further findings must close with it — the positive-weight note must stop claiming replay success beside a visible failure; the overlay preservation tests must compare complete event rows, not counts and ids; the revised demo must measure inside 60 to 90 seconds; and the replay screenshot must carry its true capture source. No task revision, redispatch or product ratification was required. **All five are corrected on the same branch**, by the coordinator, in commits `a7de7e7a7fe4bd98d56d6ec2038ac7b536d02815` (findings 1 to 3 and the narration) and `894738cbe97d0f5261f12fae54df885a6487e836` (the remeasured demo and the recaptured screenshot with its own attribution); the status commit `f944ccc4e180b458504037fa2c40c82b44303ceb` is pinned separately from both. **Accepted by the ChatGPT project reviewer on 2026-09-22 at exactly `f944ccc` (`.handoffs/review-disposition-d017-implementation-correction.md`): ACCEPTED, all five findings closed, no blocking implementation finding remains, implementation acceptance only.** Correction packet `.handoffs/review-packet-d017-implementation-correction.md`, SHA-256 `c4bc68a884284872490d69a639d6f0e547c926b1aec43f97996a1b793b9bb3a4`, verified by the reviewer. Coordinator verification at `f944ccc`: 697 passed and none failed; 591 ordinary plus 106 invariants under the CI profile; ruff check and format clean at 91 files; the eight-invocation recipe reproducing fresh digest `530f992c…35ea` with `register-current-logic` creating exactly one event and its retry a duplicate, and each later seed correctly reporting not a fresh seed; all nine pinned canonical event hashes compared at full length and matching; the demo rehearsal remeasured at 84.78 seconds over 12 segments, 18 declared targets, every narrated value on screen, no problems; the replay screenshot recaptured at commit `a7de7e7` over a dataset-plus-overlay ledger and inspected. CI run 35697912816 green on lint, tests and invariants at that exact commit. The reviewer independently re-ran the suite, the CI-profile invariants, lint, the ordered CLI recipe, the nine full-length hashes and its own collector-to-page failure probes from a `git archive` export, rehashed all 123 exported files against the reviewed blob ids, inspected the CI logs, and confirmed the retained capture and the committed image are byte-identical (SHA-256 `36f37ad8ae0b94d58184d86f228e8bed6306451c95ae4809bdf436e6c9b6c1e4`). Reviewer evidence: `.handoffs/d017-implementation-correction-review-evidence/`. Reviewer rulings carried forward so they are not relitigated: the earlier mutation results stand as historical evidence at the previous candidate and no new mutation round is required by this correction; the demo duration is closed on inspected coordinator execution evidence, which is not a recording and not human-delivery evidence; the generic-input-locator limitation stays nonblocking; and the historical capture action is coordinator-reported, with no independent recapture claimed. One earlier bounded correction was delivered outside the phase sequence and is complete: the D-016 stale-outcome write admission rule, ratified 2026-09-19 and merged 2026-09-21. **D-016 built on `correction/stale-outcome-admission` at `4a1a83f5662d89ac4314a3ff92fc1043e1a43d94` (one commit, five files: the `_validate_attribution` function in `src/flight_recorder/collector/projections.py`, plus `tests/acceptance/test_attribution_ingest.py`, `tests/acceptance/test_insights_engine.py`, `tests/conftest.py`, `tests/invariants/test_inv_08_supersession.py`; no file outside the allowed-edit list, one pre-authorized existing-test edit, no `collector/service.py` delta). Coordinator-verified 2026-09-19 against a disposable database: 660 passed, up from 654; 106 invariants under the CI profile, up from 105; ruff check and format clean at 88 files; the five-invocation recipe reproduces fresh digest `530f992c…35ea` over 1,596 items, a second seed is all duplicates at the same digest, `attribute` creates exactly the two stage-2 results at cutoff 1,596, a third seed reports not a fresh seed; all nine pinned `events.canonical_hash` values match, each resolved from its fixture's own `event_id`. CI run 35474324547 green on lint, tests and invariants at that exact commit. The refusal is HTTP 422, reason `attributed_outcome_version_is_superseded`, carrying both the submitted and the effective outcome id with the effective one named in the detail; an ambiguous chain converts to a named 422 through `_attribution_failure`; the sibling `_validate_outcome_supersession` is untouched. Accepted by the ChatGPT project reviewer on 2026-09-21 at that exact commit (`.handoffs/review-disposition-d016-implementation.md`, ACCEPTED, no blocking findings, no correction build required): the reviewer ran the full suite (660), the CI-profile invariants (106), ruff, the five-invocation recipe and all four required mutations from a `git archive` of the candidate with disposable databases, rehashed all 118 exported files against the candidate's blob ids, inspected the complete CI job logs for run 35474324547, and ran its own boundary probe, which refused an attribution computed before three consecutive outcome corrections while naming the final successor, kept a reformatted identical retry a duplicate, kept a changed-content retry a conflict, reproduced the original result field for field at its original cutoff, and confirmed a result computed before an unrelated later ledger event is still admissible while its named outcome is current, so the rule was not widened into a blanket rejection of older snapshots. Reviewer evidence: `.handoffs/d016-review-evidence/`. Merged no-ff as `bd1582b67604fc18582fee833edb51f49cb22b8c` on 2026-09-21 on the user's explicit word: "Yes—merge correction/stale-outcome-admission at 4a1a83f5662d89ac4314a3ff92fc1043e1a43d94 into main as proposed." Verified on `main` at that merge commit against a disposable database: 660 passed, 106 invariants under the CI profile, ruff clean at 88 files, the five-invocation recipe reproducing fresh digest `530f992c…35ea` with the second seed all duplicates, `attribute` creating exactly two at cutoff 1,596 and the third seed reporting not a fresh seed, and all nine pinned hashes compared one by one and matching; CI run 35672539880 green on lint, tests and invariants at `bd1582b`. The branch is kept, no pull request was used, and no deployment is authorized.** The other open items and retained findings listed below blocked none of the accepted Phase 5 gates, which is narrower than blocking nothing at all: D-016 is an example of one that was later ratified as work worth doing. Record of 5C, complete: **5C built on `phase-5/portfolio` at `8d18cf57fe9fe1311e000b4bc73cddda54866166` (four commits, five files added: `README.md`, three PNGs under `docs/screenshots/`, `tests/acceptance/test_readme.py`; no protected path touched, no existing test edited, no application source changed). Coordinator-verified and reviewer-accepted: 654 passed, 105 invariants under the CI profile, ruff clean at 88 files, CI run 35438042348 green at the accepted commit. Two review rounds produced two findings, both closed. The Merge sentence was cut back to the ratified bounded wording. The demo rehearsal was reworked twice: a 150 wpm allotment is not spoken delivery, and a first narrated run revealed one element per step and then spoke the whole clip, so it narrated the three Insights comparisons and the action and cost while the viewport sat elsewhere. The run of record splits the unchanged narration into 12 segments, scrolls to the region each is about, and verifies by bounding rectangle that its declared targets are inside a 1280 by 720 viewport before it is spoken: 82.67 seconds, 18 declared target checks, all 18 on screen, no problems. The reviewer ruled that satisfies the "polished 60 to 90 second demo path" clause as ready to record; speech is automated and that is stated, not implied. AC-01 to AC-18: seventeen closed, one closed with a stated boundary (AC-09, because the application has no explanation regeneration or hiding path to exercise); AC-17 closed outright. AC-19 is a SHOULD and remains unmeasured, because no first-time viewer was available, which is its documented reason and gates nothing. Assessment: `.handoffs/p5c-gate-assessment.md`. Merged no-ff as `e33572530dee89810af530fc72939908365140f4` on 2026-09-19 on the user's word "merge".** Record of the dispatch: task `.handoffs/phase-5-task-5c.md` revision 1 was accepted with three conditions by the ChatGPT project reviewer on 2026-09-17 at `643eb51` (`.handoffs/review-disposition-p5c-predispatch.md`: carry Q5's executed fresh setup and browser walk into acceptance; document the collector as a usable decision submission built on the canonical decision fixture; bind documentation assertions to named claims and to individual demo steps rather than to global membership). All three are closed in revision 2, and the disposition says they proceed to dispatch without another pre-dispatch round. Settled rulings carried into revision 2: the `ci.yml` workflow badge stands, linked and truthfully described, with no CI split; AC-19 is reported separately and gates nothing; the 5B signal-table layout finding stays out of scope with `src/` protected; the three PNGs are committed; an unmet §12 clause is reported, never waived or fixed by widening scope; "ready to record" is sufficient; and no completion gate can be established by that disposition. Dispatched from this commit on branch `phase-5/portfolio`. Record of 5B, complete: **5B built on `phase-5/insights-page` at `05b93690053c15b8c625abcd2c0289e2ad867bd4` (three commits, 10 files, 1,729 insertions, no protected file touched, no existing test edited): the `/insights` page renders `analytics/insights.py` at one cutoff captured once per request, and the account list pins NovaSignal AI and filters by literal name substring. Coordinator-verified 2026-09-16 in a detached worktree: 613 passed (591 existing plus 22 new); 105 invariants under the CI profile; ruff clean (86 files); the recipe reproduces fresh-seed digest `530f992c...35ea` over 1,596 items, a second seed is all duplicates, `attribute` creates exactly the two stage-2 results, a third seed reports not fresh; nine pinned hashes match; a coordinator probe written from scratch against the rendered page (unresolved observations move coverage by three and leave every rendered rate byte-identical, `%` and `_` match no account, an ambiguous selection renders a named failure with no rate, and 26 rendered values equal a direct engine call) all pass; browser-pane focus order and focus visibility on both pages; headless screenshots at `.handoffs/p5b-insights-coord.png` and `.handoffs/p5b-accounts-coord.png`. CI run 35125578741 green on lint, tests and invariants at that commit. Accepted by the ChatGPT project reviewer on 2026-09-16 from packet `.handoffs/review-packet-p5b.md` (SHA-256 `cee430e3...`): the reviewer independently ran the full suite, the CI-profile invariants, ruff, the recipe and its own collector-to-page probe from a `git archive`, and exercised Enter activation in its own browser, which closes the coordinator's reported keypress gap. AC-16 closed at this commit through `test_insights_engine.py::test_unresolved_positive_observations_never_enter_decision_metrics` (unchanged since `b75da53`) paired with `test_insights_page.py::test_unresolved_and_awaiting_observations_change_coverage_only_on_the_page`. One nonblocking P3 finding, not a correction round: the ten-column signal overview table at `insights.html:120` needs horizontal scrolling at a 1280-pixel viewport, with the data present and keyboard reachable; addressing it is a small layout improvement available to 5C. Merged no-ff as `ab42ed7ee3822995c39817ca8b6e3d6bf27a8b96` on 2026-09-16 on the user's word "merge".** Record of the dispatch: task `.handoffs/phase-5-task-5b.md` revision 1 was accepted with five conditions by the ChatGPT project reviewer on 2026-09-16 at `115d3b4` (`.handoffs/review-disposition-p5b-predispatch.md`: per-cohort exclusion counts on every rate; the eight-event NovaSignal AI trace after `seed-dataset`; a request-level single-cutoff-capture test; engine diagnostics rendered intact with the vocabulary ban limited to authored copy; literal-substring name filtering with `autoescape`); all five verified by coordinator probe and closed in revision 2 (`.handoffs/review-packet-p5b-predispatch.md` section 0); the disposition says these bounded corrections proceed to dispatch without another round. Dispatched from this commit. Record of 5A, complete: the seeded 200-account dataset as a bounded operation schedule through the collector, the `analytics/` metric functions implementing D-014's contract, and the proofs (AC-13, AC-14, AC-16's last clause, AC-11, INV-03, INV-10 and the reviewer's required-evidence boundaries). Task file `.handoffs/phase-5-task-5a.md` revision 2 accepted for dispatch by the ChatGPT project reviewer on 2026-09-14 at `a8766a5` (`.handoffs/review-disposition-p5a-predispatch-r2.md`; task SHA-256 `b2c52e00…`; revision 1 had five findings, all coordinator errors, closed in revision 2). Dispatched from `c61e163`; the builder stopped at the base with nothing written and no branch created: revision 2's rule "outcomes only after actions, no action for `DO_NOT_PRIORITIZE`" leaves effect 1 (`recently_funded`) with no known-false arm, because under `v3.2` `PRIORITIZE` requires the funding factor (positive weights 86, 68 without it, threshold 75) and `v5.1` never reaches 75 (positive weights 72). Coordinator scoping error, confirmed from the fixture weights. The revision 3 delta (outcomes recorded against a decision with no action through the decision-only reference of D-007 and D-013 Q2) was accepted with conditions by the reviewer at `57d6a73` (`.handoffs/review-disposition-p5a-delta.md`); its condition 1, the window origin for no-action observations, is ratified as D-015; conditions 2 and 3 are in task revision 4 (SHA-256 `a2aa838c…`), dispatched from `29d1d20`. **5A built on `phase-5/dataset-analytics` at `6e1b24cd386aa8f10835fc30ce7c01f37d5886d6` (four commits, 15 files, no protected file touched), coordinator-verified 2026-09-14 in a detached worktree: 591 passed; 105 invariants under the CI profile; ruff clean (82 files); the four-command recipe reproduces fresh-seed digest `530f992c…35ea` over 1,596 scheduled items in 2.6 s, a completed-seed retry is all duplicates, `attribute` creates exactly the two stage-2 results, a third seed reports not fresh; nine pinned hashes match; a coordinator probe (divergence stops with snapshot equality, retry no-op, unresolved positives changing no rate, the three effects recomputed: +1.47, +32.57, −21.39 points) all pass; CI run 34903548384 green on lint, tests and invariants at `6e1b24c`. Accepted by the ChatGPT project reviewer on 2026-09-14 from review packet `.handoffs/review-packet-p5a.md` (SHA-256 `a72ab66f…`): the reviewer independently ran the full suite (591), the CI-profile invariants (105), ruff, the four-command recipe, the nine hashes and its own probes from a `git archive` of `6e1b24c`, recovered the three effects as exact fractions (`470/319`, `29375/902`, `-56450/2639` points), and inspected CI run 34903548384; no corrections or conditions. Reviewer rulings: `WORKFLOW_UNDER_COMPARISON = "v4.2"` is a contract constant fixed by D-014, no third argument; the stage-2 correction (120-day open to 30-day closed) is a D-013 correction that does not establish completion of the original 120-day period, a distinction 5B wording must preserve; the pinned-hash file name in the task (`test_fixture_integrity.py`; the literals live in `tests/unit/test_outcome_schema_versions.py`) is an ordinary in-scope correction needing no further permission; the builder-reported "Proceed" choice remains builder-reported and the acceptance does not depend on it. Merged no-ff as `b75da53` on 2026-09-15 on the user's word "merge".** Hosting for flight-recorder.app stays parked by D-014 (no deployment work in Phase 5; the `.app` TLD is HSTS-preloaded). What exists on `main` now: the collector, the ledger, evaluator-v1 with exact reconstruction, counterfactual replay, the decision screen with its replay panel, outcome attribution with its persisted results and their full presentation.

## Open Items and Retained Findings

- **D-016 rulings, retained so they are not relitigated:** the collector's equal-cutoff branch stays unreachable through the collector and is never manufactured into a cutoff error, because the duplicate, conflict and operation-identity paths already serve that case; the sibling `_validate_outcome_supersession` stays untouched, including its unwrapped `AmbiguousSelection`; full-state comparisons in tests use the existing `ledger_state` pattern, not `Harness.snapshot()`, which stores only an event count; a rule's placement in a validation sequence is verified by source inspection, not by a test that mirrors a line's location. Two nonblocking precision notes stand uncorrected on purpose, because editing an implementation file would have fallen outside the exact-commit acceptance: a comment in the modified Insights test says "changes no rendered value" where the test proves engine-level exclusion and not browser rendering, and the legacy-row helper's "only shape" wording is not an exhaustive taxonomy of database corruption. The closed gate in `.handoffs/phase-4-task-4a-02.md` is moot and stays history.
- **Resolved as D-017, no longer an open finding:** the canonical `v5.1` artifact's positive weights sum to 72 against threshold 75, so it can output `PRIORITIZE` for no context; `v3.2` prioritizes only when the funding factor matches. First recorded in D-015, carried forward by D-016, referred to the user, and ratified on 2026-09-21 as D-017. The answer is an immutable successor, not a change to `PRODUCT.md` §7's `v5.1` weights: `v5.1` keeps its content, its hash and its exact replay, and `v5.2` supersedes it as the demo default. See the Present Objective.
- **D-017 rulings, recorded so they are not relitigated:** `v5.1` is never edited, relabelled or rejected, and neither is `v3.2`; a second factor on `verified_integration_pressure` is not permitted, because the strict model requires unique factor keys, so the pressure signal is expressed once and positively; the rejected alternative was a successor keeping `v5.1`'s six factors at threshold 65, refused on the merits because it scores 72 and outputs `PRIORITIZE` when the pressure input is absent or unavailable, making absence of evidence favourable and dropping the pressure factor from necessary to optional; registering a successor does not retire its predecessor, so two artifacts carry `status` `current` and that is described rather than corrected; the selected demo default and an artifact's recorded activation metadata are distinct and are never conflated; the reachability warning is read-side only and never becomes a collector admission rule.
- **Phase 5 rulings now carried by D-014** (metric contract, seed schedule, portfolio minimum, hosting deferral); the packet and both dispositions stay in `.handoffs/` as history.
- **Reviewer rulings from the 4A review, retained so they are not relitigated:** open-window outcomes keep receiving attribution (D-013 Q3; no `evaluation_window_still_open` reason, no evaluation state copied into attribution records); no cross-version chronology rule (a correction may legitimately describe an earlier observation time); a replacement attribution must carry a strictly newer `ingest_cutoff` (built at `1050018`, rejection `replacement_attribution_cutoff_is_not_newer`; its `<=` keeps an equality branch that is unreachable through the collector, deliberately). Carried into 4B: window state, observation values, attribution status and heuristic labelling stay distinguishable. Carried into Phase 5: resolve effective outcome versions before selecting their attribution; define every metric's population and denominator explicitly; an open-window `false` never becomes a completed negative outcome and an unknown never becomes a `false`; unresolved outcomes stay visible and out of decision-level outcome metrics; attribution-row counts alone are insufficient because they include historical and superseded results.
- **Historical record:** one edit outside 4A's allowed-edit list (`tests/invariants/test_inv_01_projections_append_only.py`, setup only) was identified during review and accepted on review; the build report's claim that it was authorized mid-task was unsupported, so every task file now requires a quotable approval for any exception. The 4A acceptance's four non-blocking reporting qualifications are recorded in this file's history at `60ba31a`.

## Verified Repository Condition

- **D-016 anchors, three commits pinned separately so none stands in for another.** The **documentation commit** is `2d5636dc804152ef051bf5508548b8d793d0b75a`: the ratified D-016 entry in `DECISIONS.md` and its status record in `STATE.md`, and nothing else. Its difference from the pre-dispatch review base `4b3ec231ae6239be41cf8c7891eeb010d6760875` is exactly those two files, so the reviewed source and tests are identical at both, and it is the approved task's base. The **implementation candidate** is `4a1a83f5662d89ac4314a3ff92fc1043e1a43d94` on `correction/stale-outcome-admission`, pushed, local and remote identical, working tree clean, five files changed against the documentation commit, 349 insertions and 11 deletions, CI run 35474324547 green on lint, tests and invariants at that exact SHA. The **merge** is `bd1582b67604fc18582fee833edb51f49cb22b8c`, a no-ff merge of the candidate into the STATE commit `50f3a769488904a185bb9ad0140e834d6980d9fc`; `main` is that merge plus this STATE commit, pushed, `origin/main` identical, working tree clean, the branch kept and fully merged, no pull request used. CI on `main` at `bd1582b`: run 35672539880, lint, tests and invariants all success. Coordinator verification of the candidate on 2026-09-21 against disposable databases: 660 passed, 106 invariants under the CI profile, ruff clean at 88 files, the five-invocation recipe reproducing fresh digest `530f992c…35ea`, and all nine pinned hashes compared one by one and matching. Re-verified on `main` after the merge on 2026-09-21 with the same results; log `.handoffs/.main-d016-verify.log`. Hash evidence: `.handoffs/.coord-d016-hashes.log` and that log, each printing `9 of 9 compared`. **The earlier `.handoffs/.coord-d016-verify.log` is kept as history and its hash line reads `all match (0 checked)`: its parser matched nothing and the pass was vacuous. Cite the counted log, never that line.** The verify script's parser is fixed so it asserts the count and fails loudly instead of passing empty. What `main` now holds from D-016: the collector refuses a new attribution naming a superseded outcome version, at the current ledger maximum, inside the same transaction, with both outcome ids in the named 422.
- Historical 5C record, superseded by the bullet above: `main` = `e33572530dee89810af530fc72939908365140f4` (the no-ff merge of `phase-5/portfolio` at `8d18cf5` into the STATE commit `3eefbaa`) plus this STATE commit; pushed; `origin/main` identical; working tree clean; checkout on `main`; the branch is fully merged, no pull request used. Verified on `main` at `e335725` from the Mac checkout against a disposable `--db` database on 2026-09-19: 654 passed; 41 of those are the documentation tests; 105 invariant under the CI profile; ruff check and format clean (88 files); `reset`, `seed-dataset` 1,596 created with fresh digest `530f992c...35ea`, second `seed-dataset` 0 created and 1,596 duplicate with the same digest, `attribute` 2 created at cutoff 1,596, third `seed-dataset` reports not a fresh seed; all nine pinned `events.canonical_hash` values match. CI on `main` at `e335725`: run 35442229622, lint / tests / invariants all success. Log: `.handoffs/.main-p5c-verify.log`. What `main` now holds from 5C: `README.md` with the architecture diagram, the collector submission example and the eight-step demo path, three screenshots under `docs/screenshots/`, and `tests/acceptance/test_readme.py` keeping the README true as the code moves.
- Historical 5B record, superseded by the bullet above: `main` = `ab42ed7ee3822995c39817ca8b6e3d6bf27a8b96` (the no-ff merge of `phase-5/insights-page` at `05b9369` into the STATE commit `f47ea90`) plus its STATE commit; pushed; `origin/main` identical; working tree clean; checkout on `main`; the branch is fully merged, no pull request used. Verified on `main` at `ab42ed7` from the Mac checkout against a disposable `--db` database on 2026-09-16: 613 passed; 105 invariant under the CI profile; ruff check and format clean (86 files); `reset`, `seed-dataset` 1,596 created with fresh digest `530f992c...35ea`, second `seed-dataset` 0 created and 1,596 duplicate with the same digest, `attribute` 2 created at cutoff 1,596 (257 already attributed), third `seed-dataset` reports not a fresh seed over 1,598 events; all nine pinned `events.canonical_hash` values match. CI on `main` at `ab42ed7`: run 35135856598. Log: `.handoffs/.main-p5b-verify.log`. What `main` now holds from 5B: the `/insights` page over `analytics/insights.py` at one cutoff captured once per request, the account list pin and literal-substring name filter, and the AC-16 engine-to-page proof.
- Historical 5A record, superseded by the bullet above: `main` = `b75da53` (the no-ff merge of `phase-5/dataset-analytics` at `6e1b24c` into the STATE commit `b4f08d6`) plus its STATE commit; pushed; working tree clean; checkout on `main`; the branch is fully merged, no pull request used. Verified on `main` at `b75da53` from the Mac checkout against a disposable `--db` database on 2026-09-15: 591 passed; 105 invariant under the CI profile; ruff check and format clean (82 files); `reset`, `seed-dataset` 1,596 created in 2.4 s with fresh digest `530f992c…35ea`, second `seed-dataset` 0 created and 1,596 duplicate with the same digest, `attribute` 2 created at cutoff 1,596 (257 already attributed), third `seed-dataset` reports not a fresh seed over 1,598 events; all nine pinned `events.canonical_hash` values read from SQLite match. CI on `main` at `b75da53`: run 34941532417, lint / tests / invariants all success, head SHA confirmed. Log: `.handoffs/.main-p5a-verify.log`. What `main` now holds from 5A: `dataset/generator.py` and `dataset/schedule.py` (the 200-account synthetic dataset as a bounded operation schedule submitted only through the collector, with divergence refusal and interruption recovery), `analytics/insights.py` (D-014's metric contract: effective observations at one cutoff, exact reconstruction of every decision, six signal states, standings, rates with unresolved and awaiting observations excluded), `fixtures/dataset/config.json` and `planted-effects.json`, the `seed-dataset` command, and the AC-13, AC-14, INV-03 and Insights-engine proofs.
- Historical 4B record, superseded by the bullet above: `main` = `22c6832` (the no-ff merge of `phase-4/presentation` at `188bc89` into the STATE commit `4a28bf0`). Verified on `main` at `22c6832` from the Mac checkout against a disposable `--db` database on 2026-09-14: 532 passed; 103 invariant under the CI profile; ruff check and format clean (73 files); `reset`, `seed` 9 created, `attribute` 1 created (canonical `direct`), second `attribute` already attributed, `--reevaluate` unchanged; all nine pinned `events.canonical_hash` values read from SQLite match. CI on `main` at `22c6832`: run 34827992859, lint / tests / invariants all success, head SHA confirmed. Pre-merge branch verification at `188bc89` on 2026-09-14 in a detached worktree with disposable databases: 532 passed; 103 invariant under the CI profile; ruff check and format clean (73 files); `--db` recipe `reset`, `seed` 9 created, `attribute` 1 created (canonical `direct`), second `attribute` already attributed, `--reevaluate` unchanged; all nine pinned `events.canonical_hash` values read from SQLite match; a probe written from scratch (4 passed); five source mutations (unbounded history query, unbounded outcome list, dropped history action, dropped history reason, dropped trace link) each caused a failure in the affected test modules, the failing test names not captured because the runs used `-x` and kept only the summary line; coordinator screenshots and a keyboard pass through the desktop browser pane (tab order, focus visibility, trace link landing); CI run 34823565726 green on lint, tests and invariants at `188bc89`. The `<details>` Enter/Space toggle was exercised by the builder through Chrome DevTools and independently by the reviewer through its in-app browser; the coordinator's browser pane could not send that keypress. The reviewer independently ran the full suite, the CI-profile invariants, ruff, the `--db` recipe, the nine hashes and its own multi-state probe, and confirmed CI run 34823565726 checked out `188bc89` in all three jobs. Five relocated assertions are the only existing-test edits; no collector, policy, schema, replay or fixture file changed.
- Historical 4A record, superseded by the bullet above: verified on `main` at `0e13591` from the Mac checkout against a disposable database on 2026-09-13: 516 passed; 103 invariant under the CI profile; ruff check and ruff format clean (72 files); `reset && seed` 9 created; `attribute` 1 created (the canonical `direct` result by `valid_source_action_reference` at cutoff 9); a second `attribute` 1 already attributed; `attribute --reevaluate` 1 unchanged; all nine pinned `events.canonical_hash` values read out of SQLite match the baselines. CI on `main` at `0e13591`: run 34808666637, lint / tests / invariants all success, head SHA confirmed. The commits after that merge touch only `STATE.md`, `AGENTS.md` and `.gitignore`.
- What exists: Python 3.13 project via uv; collector at `POST /api/v1/decision-events` with strict JSON-mode validation, canonical-JSON idempotency (200 duplicate / 409 conflict), atomic writes, account rules, six decision-envelope coherence validators, and cross-event reference and time-boundary validation at ingest (evidence `available_at` ≤ `decision_boundary`; persona/action at or after the decision boundary; outcome strictly after its action; artifact hash resolving to a registered artifact with matching identity; supersession target pre-existing). Schema-v1 event type `logic_artifact.registered` under the reserved `_system` principal (strict `LogicArtifact` model; excluded from every account-facing surface via `accounts_query()`; `/accounts/_system` → 404). SQLite ledger with `accounts`, append-only `events`, and nine append-only projection tables written atomically with each accepted event (`evidence_versions`, `logic_artifacts`, `decisions`, `decision_context`, `decision_consumed_inputs`, `persona_selections`, `actions`, `outcomes`, `outcome_attributions`), all guarded by database UPDATE/DELETE triggers. Evidence-version content identity = (`account_ref`, `evidence_type`, canonical `value_json`, `source`, `observed_at`, `available_at`, `supersedes_evidence_version_id`); exact re-mint → 422, different content → 409. Append-only evidence supersession link (`supersedes_evidence_version_id`). Canonical NovaSignal AI fixture: nine envelopes (two artifact registrations + seven account events) seeded only through the collector; the account trace still renders seven rows with the synthetic banner. `evaluator-v1` (`logic/rules.py` closed grammar over the schema-v1 artifacts, `logic/evaluator.py` pure deterministic evaluation, `replay/reconstruct.py` exact reconstruction from the projection tables with artifact presence, JSON, content hash, strict schema, row/content identity, decision/artifact identity, and runtime evaluator identity verified before evaluation; every failure explicit and named; reconstruction writes nothing and reads no `events`/`accounts` row). Full AC-15 closed; AC-01 closed (NovaSignal AI reconstructs to 86 / 75 / `PRIORITIZE` with the five stored contributions and the preserved `-v1` evidence ids); AC-04 closed for the reconstruction path; AC-07 integrity half closed; INV-01 enforced at the database on all projection tables and proven for reconstruction; INV-02 reference rule enforced at ingest and verified at reconstruction, with before/at/after coverage at both layers in `Z` and offset spellings (deterministic and generative), evidence resolved by preserved id only, naive timestamps and boundaries rejected on every path; INV-03 input states distinct (consumed / ignored / unavailable / absent); INV-04 full proof obligations closed (correction appended, both versions retained, original decision resolves to original value and provenance through reconstruction); AC-05 closed; AC-03 closed (both halves); AC-04 closed (both halves); AC-02 closed; AC-06 closed at the engine level (UI rendering is 3B); AC-07 extended to the current artifact (integrity-failure UI state is 3B); INV-06 proven for the engine; AC-18 timestamp-variation clause closed; INV-05 verified before evaluation; INV-08 strict action-before-outcome; INV-09 no manufactured certainty in evaluation; INV-11 atomicity proven across events and every projection table. Outcome attribution (4A, merged `0e13591`): the `outcome.attributed` event type under the outcome's own account with the internal attribution source; `outcome-attribution-v1` in `attribution/policy.py` (a valid explicit source reference, else the most recent eligible outbound action within 90 days with ties broken by ingest sequence, else `unresolved`; `inferred` is heuristic); collector-side recomputation of every submitted result at its `ingest_cutoff` (a submitted result the policy does not reproduce is refused); append-only `outcome_attributions` with explicit supersession, exactly one effective result per outcome version and policy, and a replacement required to carry a strictly newer cutoff; the `attribute` and `attribute --reevaluate` commands over the collector boundary (identical result = policy-result fields only); outcome schema v2 (source action and decision references kept separate from policy-resolved references, window bounds, evaluation state, as-of instant, supersession of outcome versions) with v1 untouched; the interim `#outcomes` rendering of 4A §8, truthful as text.
- Delivered in full: the collector, the ledger, evaluator-v1 with exact reconstruction, counterfactual replay, the decision screen, outcome attribution and its presentation, the 200-account dataset, the analytics engine, the Insights screen, and the portfolio materials. AC-01 to AC-18 are closed, one of them (AC-09) with a stated boundary the reviewer accepted. AC-19 is a SHOULD and remains unmeasured, because no first-time viewer was available; it gates nothing. Hosting: parked by D-014. Counterfactual persistence: none, by D-011. Attribution: scope ratified as D-013; the engine and persistence are on `main` at `0e13591` (`outcome.attributed` events, the append-only `outcome_attributions` projection, `attribution/policy.py`, `attribution/service.py`, the `attribute` command, outcome schema v2); the interim rendering of 4A §8 is truthful as text and is what 4B presents fully. Schema v1 records no evaluation-window state and cannot express a reference-free outcome observation or a decision-only reference, which is why D-013 adds outcome schema v2 rather than optional fields on v1: the collector hashes the validated model dump (`envelope.model_dump(mode="json")`), so a new defaulted field would change the canonical hash of payloads whose submitted bytes are unchanged. The outcome reference validator checks existence, account and strict ordering only, not execution status (which admits `failed`) and not the 90-day bound.

## Accepted Constraints Carried Forward (no `DECISIONS.md` entry; they enforce existing truth)

- Schema v1 keeps the closed seven-key typed evidence vocabulary through the MVP.
- `persona.selected` renders with kind label `EVENT`.
- A decision's `decision_boundary` is the decision event's occurrence instant; escalate before ever letting them differ.
- Counterfactual replay is Phase 3 (3A engine merged; 3B browser experience next), per D-011.
- `evaluator-v1` temporal semantics (accepted by review 2026-09-06, reaffirmed 2026-09-07): "observed within N days before the decision boundary" reads the linked evidence version's `observed_at` as that calendar date at `00:00:00Z` and matches when it is not after `T(d)` and the elapsed time `T(d) − o` is at most `N × 24` hours (inclusive); future observations excluded; `PRODUCT.md` leaves this precision open, so it is evaluator-v1 semantics, not product truth, and Phase 3 replay under `v5.1` inherits it.
- Rule grammar is closed: a factor `rule` is exactly one of the four shapes present in the canonical artifacts, matched in full (`fullmatch`); anything else, including a trailing newline, is an explicit `UnsupportedRule`. `missing_value_behavior` other than `no_match_contributes_zero` is refused. Boolean values fit no rule shape.
- Reconstruction semantics: `consumed` = available and referenced by a factor whether or not the rule matched; comparison with the record is set equality of (`input_key`, `evidence_version_id`, `contribution`) over consumed factors; divergence raises `ReconstructionMismatch`, never a best-effort result; `evidence_versions` is read only by the preserved id.
- Reconstruction verifies, at the context read and before any evaluation, that every preserved evidence reference (consumed or ignored) has `available_at` not after the decision's stored boundary, as a string comparison on the collector's fixed-width normalized timestamp form (`YYYY-MM-DDTHH:MM:SS.ffffffZ`, lexical order is chronological; the argument is the stored boundary text, never an arbitrary ISO string); a later reference is `IntegrityFailure("available_at")`; equality admitted; the sealed context is verified, never rebuilt from timestamps, and the supersession link is never followed. This verifies sealed-context membership; it does not guarantee detection of arbitrary database corruption (accepted by review 2026-09-08).
- A counterfactual is `R(Lc, H(d))` computed on demand by `replay(conn, decision_event_id, current_artifact_hash)` and never persisted (D-011); it is a `Counterfactual`, a type unrelated to `Reconstruction`, carrying the fixed, validated label `counterfactual`; the current artifact is verified by `verify_artifact_row` (hash, schema, row identity, evaluator identity; the decision-identity check alone is skipped) and must share the decision's class; `H(d)` is read through `load_context` with the stored boundary text, so every preserved reference is availability-verified exactly as for the original; an original that cannot be reproduced exactly yields no counterfactual; `compare` classifies each key as added / removed / unchanged / reweighted / changed by factor presence, rule text, weight, input state, match, and contribution, reports `unavailable` and `absent` missing inputs separately, and satisfies `score_delta == sum(contribution_delta)`. Engine-level only: UI rendering of these states (3B) and analytics exclusion of counterfactuals (Phase 5) remain open (accepted by review 2026-09-08).
- Reversible builder choices in force: SQLAlchemy Core; strictness applied at `validate_json(strict=True)`; `httpx` at runtime for the in-process seed; `_system` row is `name` "System (logic artifact registry)", `domain` `system.invalid`; `accounts_query()` lives in `ledger/schema.py`; `value_json` = evidence item minus its identity keys; outcome-ordering reason code `action_is_not_before_the_outcome` (ratified by review 2026-09-05); reconstruction lives in `replay/reconstruct.py` with the pure evaluator and grammar in `logic/`; `ArtifactMissing` is a sibling of `IntegrityFailure` under `ReconstructionError` (ratified by review 2026-09-06); `IntegrityFailure` carries `field`, `detail`, `stored`, `recomputed`; `context_states` is a read-only mapping; `require_aware_boundary` is public and applied at `evaluate` entry; the single `evidence_versions` read is `load_evidence_version` returning a frozen `EvidenceVersionRow` (`evidence_version_id`, `observed_at`, `available_at`); `load_context(conn, decision_event_id, decision_boundary)` takes the stored boundary text as a required parameter. From 3A: `replay/counterfactual.py` holds `replay`, `compare`, `Counterfactual`, `Comparison`, `ContributionChange`, `MissingInput`, `LogicIdentity`, `ORIGINAL_LABEL`, `COUNTERFACTUAL_LABEL`; `verify_artifact_row` in `reconstruct.py`; conftest helpers `COUNTERFACTUAL_FIELDS`, `v5_1_hash`, `canonical_evidence_ids`, `derived_artifact_envelope`, `register_derived_artifact`, `replay_under`, `assert_same_counterfactual`, `assert_same_comparison`; test-only artifacts `test-missing-inputs`, `test-removed-changed`, `v5.1-other-evaluator` never touch `fixtures/canonical/`.

## Run

```bash
uv sync && uv run flight-recorder reset && uv run flight-recorder seed-dataset && uv run flight-recorder register-current-logic && uv run flight-recorder serve
```
Run those four in that order. `register-current-logic` registers the `v5.2` overlay the replay panel defaults to (D-017) and must come after `seed-dataset`: the schedule prefix check refuses a dataset load into a ledger that already holds the overlay event. A canonical-only database may use `seed` in place of `seed-dataset`, and then cannot be extended by `seed-dataset` afterwards.
Open `http://127.0.0.1:8000/accounts/novasignal-ai`. Tests: `uv run pytest`; invariants only: `uv run pytest -m invariant`.

## Blockers

None.

## Next Action (exactly one)

The user decides whether to merge `correction/scoring-successor-v5.2` at `f944ccc4e180b458504037fa2c40c82b44303ceb` into `main`. Implementation is accepted at that exact commit and nothing further is required of the builder, the reviewer or the coordinator. **Merge and deployment are unauthorized until the user says so in his own words, and no pull request, merge, tag or deployment step may be prepared or run before that.**

Phase 5 remains complete and both completion gates remain met. Merge and deployment stay unauthorized. The other open items stay the user's, untouched: the nonblocking signal-table layout at a 1280-pixel viewport, for which an unapplied CSS diff sits at `.handoffs/launch-preparation.patch`; AC-19, closeable by showing the application to two or three people who have not seen it; and hosting, which stays parked by D-014.
