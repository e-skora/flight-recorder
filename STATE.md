# STATE.md — Current Repository State

> The only live status record. Keep it short and current: current status, outstanding limitations, the next action. Not a backlog, roadmap, or branch log. Git history holds prior states of this file; `DECISIONS.md` holds ratified decisions; the git-ignored `.handoffs/` directory holds task files, packets and dispositions as historical evidence, never as current instructions.

**Updated:** 2026-09-14

## Phase

**Phase 4, Outcome Attribution. 4A (attribution engine and persistence) is complete: accepted by the ChatGPT project reviewer at `1050018b9b58f6d4cc3923710467e39284063a8d` and merged into `main` as `0e13591` on 2026-09-13 with the user's explicit authorization. 4B (the attribution presentation) accepted by the ChatGPT project reviewer on 2026-09-14 at `188bc8912f475133d5490f151df478aaa0a56ce7` on `phase-4/presentation` (`.handoffs/review-disposition-p4b.md`; task revision 4, SHA-256 `dbe44d6e…`; packet `.handoffs/review-packet-p4b.md`): "attribution experience complete, subject to explicit acceptance and invariant mapping", the acceptance being that disposition and the mapping its section on INV-08 / AC-10, INV-09, INV-06 and INV-10, and the cutoff criterion. Keyboard ruling: automated key input that exercises real browser behavior with the resulting focus, activation and state observed is sufficient for this task; the reviewer itself toggled the history controls with Enter and Space; no physical-keyboard pass is required, and no assistive-technology audit is claimed. Not merged; merge authorization is the user's.** Phases 0 to 3 are complete and merged (Phase 3: engine `61b58c9`, screen `bce30d9`). Phase 4 scope is D-013 (ratified 2026-09-10).

Completion boundary: "attribution engine and persistence complete". Never "attribution complete" and never "AC-16 closed": 4A proves the effective resolved-outcome selection boundary, and the decision-metrics exclusion closes through Insights in Phase 5. 4B completes as "attribution experience complete, subject to explicit acceptance and invariant mapping". No deployment is authorized.

## Present Objective

4B on the existing decision screen (`.handoffs/phase-4-task-4b.md`, revision 3, pre-dispatch accepted; the four findings closed: history rows render their own stored values, outcome standing is independent of attribution-selection failure, the whole outcome section is read at one display cutoff resolved before the list is read, and the verification recipe uses a disposable `--db` database with an executed keyboard check): present each outcome version's window state, observation values, attribution status, heuristic label, source-provided claims versus policy-resolved references, whether the resolved decision is the one on the page, and the version's standing, each under its own label; list every persisted attribution result for a version with the effective one marked; link the trace's attribution rows to the credited decision; do the rendered-page, visual and keyboard checks 4A deliberately skipped. No semantics, no aggregate, no schema or policy change. Then Phase 5: Insights with the decision-metrics exclusion, the 200-account dataset, README, screenshots, the demo, and the parked flight-recorder.app hosting decision, which arrives as a decision packet for the reviewer (the `.app` TLD is HSTS-preloaded, so any host must terminate real HTTPS; no hosting work is authorized).

## Open Items and Retained Findings

- **Unratified proposal, the user's decision:** a new attribution write must target the currently effective outcome version at submission and be refused by a named stale-outcome reason otherwise. Full specification: section 3 of `.handoffs/phase-4-task-4a-02.md`, behind a closed gate at section 0. Not built: the collector still accepts an attribution for an already-superseded outcome version, which the read path excludes from every selection. 4A's acceptance is not acceptance of this rule, and it is not carried into 4B. If ratified: `DECISIONS.md` entry first, then open the gate, then dispatch section 3 as its own correction.
- **Reviewer rulings from the 4A review, retained so they are not relitigated:** open-window outcomes keep receiving attribution (D-013 Q3; no `evaluation_window_still_open` reason, no evaluation state copied into attribution records); no cross-version chronology rule (a correction may legitimately describe an earlier observation time); a replacement attribution must carry a strictly newer `ingest_cutoff` (built at `1050018`, rejection `replacement_attribution_cutoff_is_not_newer`; its `<=` keeps an equality branch that is unreachable through the collector, deliberately). Carried into 4B: window state, observation values, attribution status and heuristic labelling stay distinguishable. Carried into Phase 5: resolve effective outcome versions before selecting their attribution; define every metric's population and denominator explicitly; an open-window `false` never becomes a completed negative outcome and an unknown never becomes a `false`; unresolved outcomes stay visible and out of decision-level outcome metrics; attribution-row counts alone are insufficient because they include historical and superseded results.
- **Historical record:** one edit outside 4A's allowed-edit list (`tests/invariants/test_inv_01_projections_append_only.py`, setup only) was identified during review and accepted on review; the build report's claim that it was authorized mid-task was unsupported, so every task file now requires a quotable approval for any exception. The 4A acceptance's four non-blocking reporting qualifications are recorded in this file's history at `60ba31a`.

## Verified Repository Condition

- Branch `phase-4/presentation` = `188bc89` = origin, one commit ahead of `main` `137393e`, merge base `137393e`, no pull request. Coordinator-verified on 2026-09-14 in a detached worktree with disposable databases: 532 passed; 103 invariant under the CI profile; ruff check and format clean (73 files); `--db` recipe `reset`, `seed` 9 created, `attribute` 1 created (canonical `direct`), second `attribute` already attributed, `--reevaluate` unchanged; all nine pinned `events.canonical_hash` values read from SQLite match; a probe written from scratch (4 passed); five source mutations (unbounded history query, unbounded outcome list, dropped history action, dropped history reason, dropped trace link) each caused a failure in the affected test modules, the failing test names not captured because the runs used `-x` and kept only the summary line; coordinator screenshots and a keyboard pass through the desktop browser pane (tab order, focus visibility, trace link landing); CI run 34823565726 green on lint, tests and invariants at `188bc89`. The `<details>` Enter/Space toggle was exercised by the builder through Chrome DevTools and independently by the reviewer through its in-app browser; the coordinator's browser pane could not send that keypress. The reviewer independently ran the full suite, the CI-profile invariants, ruff, the `--db` recipe, the nine hashes and its own multi-state probe, and confirmed CI run 34823565726 checked out `188bc89` in all three jobs. Five relocated assertions are the only existing-test edits; no collector, policy, schema, replay or fixture file changed.
- `main` = `704de23` (the 4A merge `0e13591` followed by coordinator STATE, AGENTS and .gitignore commits only) plus this STATE commit; pushed; working tree clean; the checkout rests on `phase-4/presentation` between coordinator STATE edits. Historical 4A record, superseded by the 4B bullet above for current status: verified on `main` at `0e13591` from the Mac checkout against a disposable database on 2026-09-13: 516 passed; 103 invariant under the CI profile; ruff check and ruff format clean (72 files); `reset && seed` 9 created; `attribute` 1 created (the canonical `direct` result by `valid_source_action_reference` at cutoff 9); a second `attribute` 1 already attributed; `attribute --reevaluate` 1 unchanged; all nine pinned `events.canonical_hash` values read out of SQLite match the baselines. CI on `main` at `0e13591`: run 34808666637, lint / tests / invariants all success, head SHA confirmed. The commits after that merge touch only `STATE.md`, `AGENTS.md` and `.gitignore`.
- What exists: Python 3.13 project via uv; collector at `POST /api/v1/decision-events` with strict JSON-mode validation, canonical-JSON idempotency (200 duplicate / 409 conflict), atomic writes, account rules, six decision-envelope coherence validators, and cross-event reference and time-boundary validation at ingest (evidence `available_at` ≤ `decision_boundary`; persona/action at or after the decision boundary; outcome strictly after its action; artifact hash resolving to a registered artifact with matching identity; supersession target pre-existing). Schema-v1 event type `logic_artifact.registered` under the reserved `_system` principal (strict `LogicArtifact` model; excluded from every account-facing surface via `accounts_query()`; `/accounts/_system` → 404). SQLite ledger with `accounts`, append-only `events`, and nine append-only projection tables written atomically with each accepted event (`evidence_versions`, `logic_artifacts`, `decisions`, `decision_context`, `decision_consumed_inputs`, `persona_selections`, `actions`, `outcomes`, `outcome_attributions`), all guarded by database UPDATE/DELETE triggers. Evidence-version content identity = (`account_ref`, `evidence_type`, canonical `value_json`, `source`, `observed_at`, `available_at`, `supersedes_evidence_version_id`); exact re-mint → 422, different content → 409. Append-only evidence supersession link (`supersedes_evidence_version_id`). Canonical NovaSignal AI fixture: nine envelopes (two artifact registrations + seven account events) seeded only through the collector; the account trace still renders seven rows with the synthetic banner. `evaluator-v1` (`logic/rules.py` closed grammar over the schema-v1 artifacts, `logic/evaluator.py` pure deterministic evaluation, `replay/reconstruct.py` exact reconstruction from the projection tables with artifact presence, JSON, content hash, strict schema, row/content identity, decision/artifact identity, and runtime evaluator identity verified before evaluation; every failure explicit and named; reconstruction writes nothing and reads no `events`/`accounts` row). Full AC-15 closed; AC-01 closed (NovaSignal AI reconstructs to 86 / 75 / `PRIORITIZE` with the five stored contributions and the preserved `-v1` evidence ids); AC-04 closed for the reconstruction path; AC-07 integrity half closed; INV-01 enforced at the database on all projection tables and proven for reconstruction; INV-02 reference rule enforced at ingest and verified at reconstruction, with before/at/after coverage at both layers in `Z` and offset spellings (deterministic and generative), evidence resolved by preserved id only, naive timestamps and boundaries rejected on every path; INV-03 input states distinct (consumed / ignored / unavailable / absent); INV-04 full proof obligations closed (correction appended, both versions retained, original decision resolves to original value and provenance through reconstruction); AC-05 closed; AC-03 closed (both halves); AC-04 closed (both halves); AC-02 closed; AC-06 closed at the engine level (UI rendering is 3B); AC-07 extended to the current artifact (integrity-failure UI state is 3B); INV-06 proven for the engine; AC-18 timestamp-variation clause closed; INV-05 verified before evaluation; INV-08 strict action-before-outcome; INV-09 no manufactured certainty in evaluation; INV-11 atomicity proven across events and every projection table. Outcome attribution (4A, merged `0e13591`): the `outcome.attributed` event type under the outcome's own account with the internal attribution source; `outcome-attribution-v1` in `attribution/policy.py` (a valid explicit source reference, else the most recent eligible outbound action within 90 days with ties broken by ingest sequence, else `unresolved`; `inferred` is heuristic); collector-side recomputation of every submitted result at its `ingest_cutoff` (a submitted result the policy does not reproduce is refused); append-only `outcome_attributions` with explicit supersession, exactly one effective result per outcome version and policy, and a replacement required to carry a strictly newer cutoff; the `attribute` and `attribute --reevaluate` commands over the collector boundary (identical result = policy-result fields only); outcome schema v2 (source action and decision references kept separate from policy-resolved references, window bounds, evaluation state, as-of instant, supersession of outcome versions) with v1 untouched; the interim `#outcomes` rendering of 4A §8, truthful as text.
- Not yet: the attribution presentation (4B: the decision screen's outcome and attribution states with rendered-page and visual checks), Insights with the decision-metrics exclusion, the 200-account dataset, README. Counterfactual persistence: none, by D-011. Attribution: scope ratified as D-013; the engine and persistence are on `main` at `0e13591` (`outcome.attributed` events, the append-only `outcome_attributions` projection, `attribution/policy.py`, `attribution/service.py`, the `attribute` command, outcome schema v2); the interim rendering of 4A §8 is truthful as text and is what 4B presents fully. `analytics/` holds only an empty `__init__.py`. Schema v1 records no evaluation-window state and cannot express a reference-free outcome observation or a decision-only reference, which is why D-013 adds outcome schema v2 rather than optional fields on v1: the collector hashes the validated model dump (`envelope.model_dump(mode="json")`), so a new defaulted field would change the canonical hash of payloads whose submitted bytes are unchanged. The outcome reference validator checks existence, account and strict ordering only, not execution status (which admits `failed`) and not the 90-day bound.

## Accepted Constraints Carried Forward (no `DECISIONS.md` entry; they enforce existing truth)

- Schema v1 keeps the closed seven-key typed evidence vocabulary through the MVP.
- `persona.selected` renders with kind label `EVENT`.
- A decision's `decision_boundary` is the decision event's occurrence instant; escalate before ever letting them differ.
- Counterfactual replay is Phase 3 (3A engine merged; 3B browser experience next), per D-011.
- `evaluator-v1` temporal semantics (accepted by review 2026-09-06, reaffirmed 2026-09-07): "observed within N days before the decision boundary" reads the linked evidence version's `observed_at` as that calendar date at `00:00:00Z` and matches when it is not after `T(d)` and the elapsed time `T(d) − o` is at most `N × 24` hours (inclusive); future observations excluded; `PRODUCT.md` leaves this precision open, so it is evaluator-v1 semantics, not product truth, and Phase 3 replay under `v5.1` inherits it.
- Rule grammar is closed: a factor `rule` is exactly one of the four shapes present in the canonical artifacts, matched in full (`fullmatch`); anything else, including a trailing newline, is an explicit `UnsupportedRule`. `missing_value_behavior` other than `no_match_contributes_zero` is refused. Boolean values fit no rule shape.
- Reconstruction semantics: `consumed` = available and referenced by a factor whether or not the rule matched; comparison with the record is set equality of (`input_key`, `evidence_version_id`, `contribution`) over consumed factors; divergence raises `ReconstructionMismatch`, never a best-effort result; `evidence_versions` is read only by the preserved id.
- Reconstruction verifies, at the context read and before any evaluation, that every preserved evidence reference (consumed or ignored) has `available_at` not after the decision's stored boundary, as a string comparison on the collector's fixed-width normalized timestamp form (`YYYY-MM-DDTHH:MM:SS.ffffffZ`, lexical order is chronological; the argument is the stored boundary text, never an arbitrary ISO string); a later reference is `IntegrityFailure("available_at")`; equality admitted; the sealed context is verified, never rebuilt from timestamps, and the supersession link is never followed. This verifies sealed-context membership; it does not guarantee detection of arbitrary database corruption (accepted by review 2026-09-08).
- A counterfactual is `R(Lc, H(d))` computed on demand by `replay(conn, decision_event_id, current_artifact_hash)` and never persisted (D-011); it is a `Counterfactual`, a type unrelated to `Reconstruction`, carrying the fixed, validated label `counterfactual`; the current artifact is verified by `verify_artifact_row` (hash, schema, row identity, evaluator identity; the decision-identity check alone is skipped) and must share the decision's class; `H(d)` is read through `load_context` with the stored boundary text, so every preserved reference is availability-verified exactly as for the original; an original that cannot be reproduced exactly yields no counterfactual; `compare` classifies each key as added / removed / unchanged / reweighted / changed by factor presence, rule text, weight, input state, match, and contribution, reports `unavailable` and `absent` missing inputs separately, and satisfies `score_delta == sum(contribution_delta)`. Engine-level only: UI rendering of these states (3B) and analytics exclusion of counterfactuals (Phase 5) remain open (accepted by review 2026-09-08).
- Reversible builder choices in force: SQLAlchemy Core; strictness applied at `validate_json(strict=True)`; `httpx` at runtime for the in-process seed; `_system` row is `name` "System (logic artifact registry)", `domain` `system.invalid`; `accounts_query()` lives in `ledger/schema.py`; `value_json` = evidence item minus its identity keys; outcome-ordering reason code `action_is_not_before_the_outcome` (ratified by review 2026-09-05); reconstruction lives in `replay/reconstruct.py` with the pure evaluator and grammar in `logic/`; `ArtifactMissing` is a sibling of `IntegrityFailure` under `ReconstructionError` (ratified by review 2026-09-06); `IntegrityFailure` carries `field`, `detail`, `stored`, `recomputed`; `context_states` is a read-only mapping; `require_aware_boundary` is public and applied at `evaluate` entry; the single `evidence_versions` read is `load_evidence_version` returning a frozen `EvidenceVersionRow` (`evidence_version_id`, `observed_at`, `available_at`); `load_context(conn, decision_event_id, decision_boundary)` takes the stored boundary text as a required parameter. From 3A: `replay/counterfactual.py` holds `replay`, `compare`, `Counterfactual`, `Comparison`, `ContributionChange`, `MissingInput`, `LogicIdentity`, `ORIGINAL_LABEL`, `COUNTERFACTUAL_LABEL`; `verify_artifact_row` in `reconstruct.py`; conftest helpers `COUNTERFACTUAL_FIELDS`, `v5_1_hash`, `canonical_evidence_ids`, `derived_artifact_envelope`, `register_derived_artifact`, `replay_under`, `assert_same_counterfactual`, `assert_same_comparison`; test-only artifacts `test-missing-inputs`, `test-removed-changed`, `v5.1-other-evaluator` never touch `fixtures/canonical/`.

## Run

```bash
uv sync && uv run flight-recorder reset && uv run flight-recorder seed && uv run flight-recorder serve
```
Open `http://127.0.0.1:8000/accounts/novasignal-ai`. Tests: `uv run pytest`; invariants only: `uv run pytest -m invariant`.

## Blockers

None.

## Next Action (exactly one)

Put the merge of `phase-4/presentation` at `188bc89` into `main` to the user as one question. On his explicit word: merge no-ff from the Mac shell, verify on `main` (full suite, CI-profile invariants, ruff, the `--db` recipe, the nine pinned hashes), check CI on `main`, and record the merge here in three sentences or fewer. Then Phase 5 scoping begins with a decision packet for the reviewer (Insights with the decision-metrics exclusion, the 200-account dataset, README, screenshots, demo, and the parked flight-recorder.app hosting decision). The stale-outcome proposal above stays open until the user rules on it. No deployment is authorized.
