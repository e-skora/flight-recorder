# STATE.md — Current Repository State

> The only live status record. Keep it short and current. Not a backlog, roadmap, or branch log.

**Updated:** 2026-09-08

## Phase

**Phase 2 — Historical Decision Core: COMPLETE (2A, 2B, 2C all merged). Phase 3 — Counterfactual Replay: scoped as D-011 (3A engine, 3B browser experience); 3A ACCEPTED by the ChatGPT project reviewer 2026-09-08 at `phase-3/replay` `3f46a90` ("counterfactual engine complete"; three commits on `main` `24e4863`, coordinator-verified 2026-09-08); merge awaits the user's authorization; not merged.** 2C built on `phase-2/corrections` at `53df754` (five commits on `main` `9aab4d0`, 2026-09-08), coordinator-verified, task text pre-dispatch reviewed (ACCEPT WITH CONDITIONS, both met), accepted by the ChatGPT project reviewer 2026-09-08 with no corrections, merge authorized by the user, merged into `main` as `9b73de0` (no-ff) on 2026-09-08. 2B built on `phase-2/evaluator` at `20e75ca`, reviewed CHANGES REQUIRED 2026-09-06 (two narrow gaps), corrected in `2558803` (task P2-B-02), accepted by the ChatGPT project reviewer 2026-09-07, merge authorized by the user, merged into `main` as `04222b3` (no-ff) on 2026-09-07. 2A accepted by ChatGPT/Codex re-review at `816b2db` on 2026-09-05, merge authorized by the user, merged into `main` as `a9a9cc4` (no-ff) on 2026-09-05. Phase 1 (Skeleton and One Trace) complete: accepted at `071da0a`, merged as `ffa42bd` on 2026-09-04. Phase 0 completed at `2cdb8b5`; stack ratified (D-010) at `d183f60`.

## Present Objective

Phase 2 in three narrow, sequential tasks, each reviewed before the next:

- **2A (done, merged `a9a9cc4`)** — domain projections (evidence versions, historical context, consumed inputs, logic artifacts, decisions, actions, outcomes) written atomically from accepted events; cross-event reference and time-boundary validation; database-level append-only protection for every projected historical record; the append-only evidence-correction/supersession representation. Closes full AC-15.
- **2B (done, merged `04222b3`)** — `evaluator-v1`: closed rule grammar over the schema-v1 artifacts (`logic/rules.py`), pure deterministic evaluation (`logic/evaluator.py`), exact reconstruction from the projection tables (`replay/reconstruct.py`) that verifies artifact presence, JSON, content hash, strict schema, row/content identity, decision/artifact identity, and runtime evaluator identity before evaluating; NovaSignal AI reconstructs to 86 / 75 / `PRIORITIZE` with the five stored contributions and the preserved `-v1` evidence ids (AC-01, INV-05); reconstruction writes nothing, reads no `events`/`accounts`, and is unchanged by later evidence, a second account, or a new artifact (AC-04, INV-01); every integrity failure is explicit and named (AC-07 integrity half, INV-09). Rule grammar closed with `fullmatch`; boundary awareness validated at `evaluate` entry.
- **2C (done, merged `9b73de0`)** — correction immutability (AC-05, INV-04 full proof obligations) and the before/at/after boundary suite (AC-03 reconstruction half, INV-02, AC-18 timestamp variation) proven at the reconstruction level: after the canonical correction, an observation-date correction, a correction to an ignored input, and a two-step chain, the canonical decision reconstructs unchanged field by field, every consumed factor resolves to its `-v1` version, value, and provenance, its projection rows are byte-identical, and a later decision reconstructs with the corrected version while the original keeps the original; evidence is read by preserved id only (proven by captured statements); unreferenced evidence at `T − 1 ms`, `T`, `T + 1 ms` in `Z` and offset spellings never enters the sealed context, a reference at or before `T` reconstructs, a reference after `T` is rejected in every spelling, an offset-spelled boundary is the same instant, a naive timestamp never enters the ledger; generative case admits a reference at `T + delta` iff `delta ≤ 0`. One code change: `load_context` takes the stored boundary text and refuses any preserved reference, consumed or ignored, whose `available_at` is after it with `IntegrityFailure("available_at")` before evaluation; equality admitted.

Phase 2 is complete. Phase 3 is split by D-011 (ratified 2026-09-08): **3A** — the counterfactual engine (`replay` under an explicitly selected, verified current artifact over the same sealed `H(d)`; a structured comparison; proofs for AC-02, AC-06, the counterfactual halves of AC-03 and AC-04, AC-07 for the current artifact, INV-06), computed on demand and never persisted; **3B** — the smallest browser experience (one decision-detail screen with a replay panel, §4.4 and §4.5 together), required product work. 3A completion means "counterfactual engine complete", not "replay experience complete".

- **3A (accepted at `phase-3/replay` `3f46a90`, 2026-09-08; not yet merged)** — the counterfactual engine: `replay(conn, decision_event_id, current_artifact_hash)` reproduces the original exactly, verifies the explicitly selected current artifact with the same checks minus decision identity (`verify_artifact_row`, split out of `verify_artifact` with no behavior change), reads the same sealed `H(d)` through `load_context`, and returns a `Counterfactual` (distinct type, fixed validated label, never persisted); `compare` exposes both logic identities, scores, thresholds, outputs, every contribution change with its evidence reference and input state on each side, and every missing input with `unavailable` and `absent` distinct. NovaSignal AI under `v5.1` replays to 51 / 75 / `DO_NOT_PRIORITIZE` through the application path with all six factors on their `-v1` versions; the comparison explains the entire −35 as funding reweighted +18 → +4 and low integration pressure added at −21 (AC-02); test-only artifacts prove `removed` and `changed` (−58), that unavailable and absent inputs stay missing at 51, and that a decision lacking a supported input replays at 72 with neither a pre-boundary nor a later version filling the gap (AC-06, INV-02, INV-03, INV-09); the counterfactual halves of AC-03 and AC-04 and the INV-02 late-reference case hold; INV-06 proven (type, label, nothing written, evidence by preserved id only, random current logic never moves the original).

## Verified Repository Condition

- `main` = `24e4863` (D-011 and pre-dispatch STATE commits on the Phase 2 merge `9b73de0`) plus this STATE commit; pushed; working tree clean. `phase-3/replay` = `3f46a90` (three commits on `24e4863`), pushed, not merged. Coordinator verification of `3f46a90` in a temporary worktree (2026-09-08 Pacific): `uv run pytest` 320 passed (271 acceptance/unit, 49 invariant; 290 → 320); `HYPOTHESIS_PROFILE=ci` invariants 49 passed in 52 s; `ruff check` and `ruff format --check` clean; `reset && seed` → 9 created, second `seed` → 0 created, 9 duplicate. Branch CI at `3f46a90`: run 34282513748, `lint` / `tests` / `invariants` green. Last verification on `main` (`9b73de0`, before the two STATE-only commits): 290 passed, CI run 34255587647 green.
- What exists: Python 3.13 project via uv; collector at `POST /api/v1/decision-events` with strict JSON-mode validation, canonical-JSON idempotency (200 duplicate / 409 conflict), atomic writes, account rules, six decision-envelope coherence validators, and cross-event reference and time-boundary validation at ingest (evidence `available_at` ≤ `decision_boundary`; persona/action at or after the decision boundary; outcome strictly after its action; artifact hash resolving to a registered artifact with matching identity; supersession target pre-existing). Schema-v1 event type `logic_artifact.registered` under the reserved `_system` principal (strict `LogicArtifact` model; excluded from every account-facing surface via `accounts_query()`; `/accounts/_system` → 404). SQLite ledger with `accounts`, append-only `events`, and eight append-only projection tables written atomically with each accepted event (`evidence_versions`, `logic_artifacts`, `decisions`, `decision_context`, `decision_consumed_inputs`, `persona_selections`, `actions`, `outcomes`), all guarded by database UPDATE/DELETE triggers. Evidence-version content identity = (`account_ref`, `evidence_type`, canonical `value_json`, `source`, `observed_at`, `available_at`, `supersedes_evidence_version_id`); exact re-mint → 422, different content → 409. Append-only evidence supersession link (`supersedes_evidence_version_id`). Canonical NovaSignal AI fixture: nine envelopes (two artifact registrations + seven account events) seeded only through the collector; the account trace still renders seven rows with the synthetic banner. `evaluator-v1` (`logic/rules.py` closed grammar over the schema-v1 artifacts, `logic/evaluator.py` pure deterministic evaluation, `replay/reconstruct.py` exact reconstruction from the projection tables with artifact presence, JSON, content hash, strict schema, row/content identity, decision/artifact identity, and runtime evaluator identity verified before evaluation; every failure explicit and named; reconstruction writes nothing and reads no `events`/`accounts` row). Full AC-15 closed; AC-01 closed (NovaSignal AI reconstructs to 86 / 75 / `PRIORITIZE` with the five stored contributions and the preserved `-v1` evidence ids); AC-04 closed for the reconstruction path; AC-07 integrity half closed; INV-01 enforced at the database on all projection tables and proven for reconstruction; INV-02 reference rule enforced at ingest and verified at reconstruction, with before/at/after coverage at both layers in `Z` and offset spellings (deterministic and generative), evidence resolved by preserved id only, naive timestamps and boundaries rejected on every path; INV-03 input states distinct (consumed / ignored / unavailable / absent); INV-04 full proof obligations closed (correction appended, both versions retained, original decision resolves to original value and provenance through reconstruction); AC-05 closed; AC-03 closed for reconstruction only (counterfactual half is Phase 3); AC-18 timestamp-variation clause closed; INV-05 verified before evaluation; INV-08 strict action-before-outcome; INV-09 no manufactured certainty in evaluation; INV-11 atomicity proven across events and every projection table.
- Not yet: 3A merge (awaiting review), decision inspection page with replay panel (3B), attribution, Insights, 200-account dataset, README. Counterfactual persistence: none, by D-011.

## Accepted Constraints Carried Forward (no `DECISIONS.md` entry; they enforce existing truth)

- Schema v1 keeps the closed seven-key typed evidence vocabulary through the MVP.
- `persona.selected` renders with kind label `EVENT`.
- A decision's `decision_boundary` is the decision event's occurrence instant; escalate before ever letting them differ.
- Counterfactual replay remains Phase 3.
- `evaluator-v1` temporal semantics (accepted by review 2026-09-06, reaffirmed 2026-09-07): "observed within N days before the decision boundary" reads the linked evidence version's `observed_at` as that calendar date at `00:00:00Z` and matches when it is not after `T(d)` and the elapsed time `T(d) − o` is at most `N × 24` hours (inclusive); future observations excluded; `PRODUCT.md` leaves this precision open, so it is evaluator-v1 semantics, not product truth, and Phase 3 replay under `v5.1` inherits it.
- Rule grammar is closed: a factor `rule` is exactly one of the four shapes present in the canonical artifacts, matched in full (`fullmatch`); anything else, including a trailing newline, is an explicit `UnsupportedRule`. `missing_value_behavior` other than `no_match_contributes_zero` is refused. Boolean values fit no rule shape.
- Reconstruction semantics: `consumed` = available and referenced by a factor whether or not the rule matched; comparison with the record is set equality of (`input_key`, `evidence_version_id`, `contribution`) over consumed factors; divergence raises `ReconstructionMismatch`, never a best-effort result; `evidence_versions` is read only by the preserved id.
- Reconstruction verifies, at the context read and before any evaluation, that every preserved evidence reference (consumed or ignored) has `available_at` not after the decision's stored boundary, as a string comparison on the collector's fixed-width normalized timestamp form (`YYYY-MM-DDTHH:MM:SS.ffffffZ`, lexical order is chronological; the argument is the stored boundary text, never an arbitrary ISO string); a later reference is `IntegrityFailure("available_at")`; equality admitted; the sealed context is verified, never rebuilt from timestamps, and the supersession link is never followed. This verifies sealed-context membership; it does not guarantee detection of arbitrary database corruption (accepted by review 2026-09-08).
- Reversible builder choices in force: SQLAlchemy Core; strictness applied at `validate_json(strict=True)`; `httpx` at runtime for the in-process seed; `_system` row is `name` "System (logic artifact registry)", `domain` `system.invalid`; `accounts_query()` lives in `ledger/schema.py`; `value_json` = evidence item minus its identity keys; outcome-ordering reason code `action_is_not_before_the_outcome` (ratified by review 2026-09-05); reconstruction lives in `replay/reconstruct.py` with the pure evaluator and grammar in `logic/`; `ArtifactMissing` is a sibling of `IntegrityFailure` under `ReconstructionError` (ratified by review 2026-09-06); `IntegrityFailure` carries `field`, `detail`, `stored`, `recomputed`; `context_states` is a read-only mapping; `require_aware_boundary` is public and applied at `evaluate` entry; the single `evidence_versions` read is `load_evidence_version` returning a frozen `EvidenceVersionRow` (`evidence_version_id`, `observed_at`, `available_at`); `load_context(conn, decision_event_id, decision_boundary)` takes the stored boundary text as a required parameter.

## Run

```bash
uv sync && uv run flight-recorder reset && uv run flight-recorder seed && uv run flight-recorder serve
```
Open `http://127.0.0.1:8000/accounts/novasignal-ai`. Tests: `uv run pytest`; invariants only: `uv run pytest -m invariant`.

## Blockers

None.

## Next Action (exactly one)

On the user's authorization, merge `phase-3/replay` at `3f46a90` `--no-ff` into `main`, re-verify on `main`, record the 3A accepted constraints and the merge in this file (with the reviewer's precision edit to the comparison wording), then scope 3B (one decision-detail screen with a replay panel: historical evidence, contribution differences, missing-input states, visible integrity failures; fix the stale `replay/__init__.py` docstring there) as `.handoffs/phase-3-task-3b.md`.
