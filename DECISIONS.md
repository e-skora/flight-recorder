# GTM Flight Recorder — Decisions

> **Purpose:** Durable record of material product and architecture decisions, with rationale and provenance.  
> **Rule:** Record only decisions actually established by the user, the ratified sources, or a later user-ratified decision packet. Do not invent decisions to fill gaps; unresolved choices stay in the "Open" section until decided. Superseding a decision appends a new entry that references the old one; entries are never rewritten in place.  
> **Sources referenced:** `02` = `02-MVP-CONTRACT.md` v1.2 (ratified 2026-09-01); `05` = `05-CORE-INVARIANTS.md` v1.1 (ratified 2026-09-01); `04` = `04-PRODUCT-PRINCIPLES.md`; `03` = `03-DUAL-MODEL-BUILD-PLAYBOOK.md` (process only).

## Ratified Decisions

### D-001 — Product form: web application with a versioned decision-event ingestion API

- **Decision:** The MVP is a web application plus a small collector API at `POST /api/v1/decision-events`. It is not fundamentally an MCP server; an MCP or agent-query interface is a possible post-MVP extension and is an explicit MVP non-goal.
- **Why:** The proof is that a real workflow could instrument a decision and an operator could inspect and replay it. An MCP surface would add a second interface without strengthening that proof.
- **Provenance:** `02` §2 (Product Form), §5.1, §11. Status: ratified 2026-09-01.

### D-002 — Primary user and organizational ICP

- **Decision:** The primary user is a GTM engineer, technical RevOps operator, or revenue-systems owner responsible for automated account-prioritization logic at a scaling B2B SaaS company. The organizational ICP is the category (lean technical GTM team, thousands of accounts, signal-based automation, fragmented stack, changing logic, evaluable outcomes; ~75–500 employees as design center), not a named company.
- **Why:** A narrow, technical primary user makes the semantics (provenance, versioning, replay) the product rather than dashboard polish, and keeps the ICP from collapsing into one reference company.
- **Provenance:** `02` §2. Status: ratified 2026-09-01.

### D-003 — Exactly one fully replayable decision class: account prioritization

- **Decision:** The MVP fully supports replay for account prioritization (`PRIORITIZE` / `DO_NOT_PRIORITIZE`) only. Discovery, enrichment, persona selection, outbound action, and outcome events are required in the trace but are not independently replayable. Additional replayable classes are post-MVP.
- **Why:** One decision implemented completely demonstrates more engineering and product judgment than several implemented superficially.
- **Provenance:** `02` §4; `04` principle 5. Status: ratified 2026-09-01.

### D-004 — Canonical fixture: RelayBridge evaluates NovaSignal AI; Merge is a public reference archetype only

- **Decision:** The fictional demo operating company is **RelayBridge** (B2B integration and agent-connectivity platform). The canonical fictional prospect is **NovaSignal AI**. **Merge** is cited only as public evidence that the operating archetype exists and MUST NOT appear as a customer, tenant, or ledger account, nor be described as lacking internal observability. The canonical facts (boundary `2026-04-17T10:05:02Z`, employees 184, industry B2B AI Software, US HQ, Series B 18 days prior, 7 open platform-engineering roles, Head of Platform joined 43 days prior, verified integration pressure LOW, workflow `v4.2`; `v3.2` → 86 / `PRIORITIZE`; `v5.1` → 51 / `DO_NOT_PRIORITIZE`; threshold 75; play `#14`, cost `$1.42`, 90-day window, direct attribution, no reply/meeting/opportunity; no confidence emitted) are fixed in `PRODUCT.md` §7 and MUST come from one canonical source in the codebase.
- **Why:** A consistent fictional company avoids overclaiming a customer relationship while keeping the demo grounded in a verified real-world pattern. Fixing the numbers makes AC-01/AC-02 exact and keeps UI, tests, screenshots, and README aligned.
- **Supersedes:** The Acme Robotics / Industrial Automation / VP Sales / 82%-confidence example used in `01-GTM-FLIGHT-RECORDER-SPEC.md`, `03` §4 and §10, `04` principle 9, and `06-PORTFOLIO-OBJECTIVE.md` §5 and §11. Those references are lower-authority illustrations and are superseded wherever they conflict.
- **Provenance:** `02` §2 (roles table), §5.2, §5.7, §8; `05` INV-03 and INV-10. Status: ratified 2026-09-01.

### D-005 — Deterministic core; scoring logic stored as immutable declarative data with hash and evaluator identity

- **Decision:** The replayed decision class uses deterministic logic. Scoring logic is stored as immutable declarative artifacts (factors, weights, missing-value behavior, threshold, output mapping, schema version, activation metadata, canonical content hash, evaluator-code version). Changing any of these yields a new identity. Exact replay verifies hash and evaluator identity first and fails explicitly otherwise. No LLM call is required in the core decision or replay path; any LLM output is labeled explanation, never evidence.
- **Why:** Exact reproduction of a historical decision is the product's central claim; a version label pointing at mutable code cannot support it, and a nondeterministic core cannot be tested against the acceptance criteria.
- **Provenance:** `02` §6.4, §6.5, §6.9, §11, §12; `05` INV-05, INV-07; `04` principle 6. Status: ratified 2026-09-01.

### D-006 — Collector contract: versioned envelope, canonical-JSON idempotency, atomic failure, seed enters through the collector

- **Decision:** One ingestion boundary, `POST /api/v1/decision-events`, with envelope fields `schema_version`, `event_id`, `event_type`, `source`, `account_ref`, `occurred_at`, `recorded_at`, `payload`. Idempotency is defined by equality after schema validation and canonical JSON serialization; conflicting reuse of an `event_id` and invalid envelopes fail visibly without partial writes. The canonical demo seed enters through this boundary (tests may call the underlying service directly). At least one documented example shows a Clay HTTP step, n8n workflow, or equivalent submitting a decision.
- **Why:** Instrumentability is one of the five proof claims; if the demo data bypassed the collector, the boundary would be untested theater.
- **Provenance:** `02` §5.1, AC-15; `05` INV-11. Status: ratified 2026-09-01.

### D-007 — Outcome attribution policy `outcome-attribution-v1`

- **Decision:** Decision-level credit uses a named, versioned policy: (1) valid explicit source-provided action/decision reference; (2) otherwise the most recent eligible outbound action for the same account before the outcome and within 90 days; (3) otherwise unresolved. Eligible actions are actual, recorded, sent/completed outbound actions linked to a prioritization decision for the same account; counterfactual and failed-to-execute actions are ineligible. Results record policy version, method, window, references, time, and status `direct` / `inferred` / `unresolved`. Unresolved outcomes stay visible and out of decision-level metrics; inferred attribution is labeled heuristic.
- **Why:** Silently crediting every account outcome to every preceding decision would make the Insights view dishonest and blur original and counterfactual results.
- **Provenance:** `02` §6.7, §6.8, AC-16; `05` INV-08. Status: ratified 2026-09-01.

### D-008 — Synthetic data only; seeded deterministic dataset with three planted, manifest-declared effects

- **Decision:** All Apollo-, Clay-, CRM-, and outcome-like events are simulated and disclosed as such in product and README. No live third-party API or paid credential is used. The dataset is deterministic and seeded, with at least 200 accounts, and plants exactly three documented effects (neutral `recently_funded`; positive `verified_integration_pressure = HIGH`; `v4.2` workflow underperformance) whose expected values live in a machine-readable manifest separate from the analytics code. Analytics must recover them from ingested events; the UI must not hard-code the conclusions.
- **Why:** Synthetic integrations prove the architecture without spending the MVP on credentials and plumbing, and planted effects make the Insights calculations verifiable rather than decorative — provided they are labeled as demonstration data, not market findings.
- **Provenance:** `02` §5.7, §9, AC-12, AC-13, AC-14; `05` INV-10; `04` principles 7 and 8. Status: ratified 2026-09-01.

### D-009 — Repository truth layer and authority after distillation

- **Decision:** From 2026-09-02 the repository and Git are authoritative for durable product truth, decisions, implementation state, and continuity. The truth layer is `PRODUCT.md` (product truth), `DECISIONS.md` (this file), `AGENTS.md` (coding-agent operating rules), and `STATE.md` (current phase and next action). The eight ChatGPT Project source files are not copied into the repository. Phase 0 deliberately creates no README, application or test scaffolding, architecture/data-model/demo-scenario/build-ledger docs, archived source copies, review-packet copy, or planning artifacts; those arrive only when real work justifies them.
- **Why:** A fresh session must recover what the product means and what to do next from repository evidence, not conversation. Empty scaffolding is not continuity, and duplicated sources create competing versions of reality.
- **Provenance:** `02` header (authority clause) and §14; `03` §4–§5 and §13 (process); user's explicit Phase 0 instruction, 2026-09-02, which narrows `03`'s suggested initial layout (README, `docs/`, `app/`, `tests/`) to the four control files for this initialization. Status: ratified by user instruction 2026-09-02.

### D-010 — Implementation stack, tooling, and repository layout

- **Decision:**
  - **Language:** Python, pinned to 3.13 via `.python-version`.
  - **Web framework:** FastAPI with Pydantic v2 for envelope validation and server-rendered HTML via Jinja2 templates. No JavaScript framework and no bundler; small amounts of vanilla JS or HTMX MAY be added inside a task if a page needs it. FastAPI's generated OpenAPI page serves as the published collector schema.
  - **Database:** SQLite, one file, accessed through SQLAlchemy 2.0. Timestamp convention (binding, because SQLite has no timezone-aware type): every timestamp is validated as timezone-aware at the Pydantic boundary, converted to UTC, and stored as ISO-8601 text with an explicit `Z`; naive datetimes are rejected. Same-instant ordering at a decision boundary uses a recorded sequence number, never display precision. Protected records (decisions, evidence versions, historical context, consumed inputs, original explanations) are append-only in code and additionally guarded by SQLite triggers that raise on UPDATE/DELETE, so a violation fails visibly. Schema is created from SQLAlchemy metadata; a fresh seeded reset is the only "migration" for the MVP. Alembic is added only if a persisted database must survive a schema change.
  - **Tests:** pytest with Hypothesis for property-based and generative cases; FastAPI `TestClient` (httpx) for collector contract tests. Tests are named by contract ID under `tests/invariants/` (INV-01…INV-11) and `tests/acceptance/` (AC-01…AC-18). Hypothesis uses a modest local profile and a larger CI profile.
  - **Lint/format:** ruff only. No type checker for now.
  - **CI:** GitHub Actions on push and pull request: install uv → `uv sync` → `ruff check` → `pytest`, with the invariant suite as a separately named job so its result is visible by name. A README badge references it (Phase 5).
  - **Project tooling:** uv manages the Python version, `pyproject.toml`, and `uv.lock`; documented setup is install uv → `uv sync` → `uv run`.
  - **Layout:** `src/flight_recorder/` with modules mirroring `PRODUCT.md` §6 concepts (`collector/`, `ledger/`, `logic/`, `replay/`, `attribution/`, `analytics/`, `web/`); `fixtures/canonical/` (NovaSignal AI trace as collector envelopes plus logic artifacts `v3.2` and `v5.1`) is the single canonical source for shared demo constants that code and tests load and never re-declare; `fixtures/dataset/` holds the seeded generator config and planted-effects manifest; `fixtures/examples/` holds the Clay / n8n example payload; `scripts/` holds seed (through the collector) and reset; `tests/invariants/`, `tests/acceptance/`, `tests/unit/`; `.github/workflows/ci.yml`. `README.md` arrives in Phase 5.
- **Why:** Python is the working language of GTM engineering and the one the builder can defend; Hypothesis is the strongest property-based testing library available and AC-18 requires generative cases. FastAPI gives schema validation before any write and a documented collector contract for free. SQLite with no server satisfies "run locally from documented steps", "no production credentials", "fresh seeded reset", and "minimize moving parts" directly, and principle 10 forbids infrastructure for hypothetical scale; SQLAlchemy keeps a later PostgreSQL switch a configuration change. GitHub Actions provides the visible CI evidence the portfolio gate requires on the repository that already exists. The `src` layout and ID-named tests make invariant tests easy to locate, as `PRODUCT.md` §11 asks.
- **Tradeoffs accepted:** server-rendered pages are less interactive than a React front end; SQLite carries a weaker production signal than PostgreSQL and requires the explicit timestamp convention above; Hypothesis tests are slower than example tests; uv is one more tool for a reader to recognize.
- **Rejected alternatives:** TypeScript / Next.js / Vitest / fast-check (richer UI, weaker generative testing, build pipeline, less defensible for the builder); Django (unneeded ORM/admin/migration weight); Flask (validation and API docs would be hand-built); PostgreSQL (setup and CI service cost without strengthening the proof).
- **Provenance:** Recommended by the coordinating Claude session and approved by the user on 2026-09-02 as the choice among options `02` §12 leaves open ("any reasonable web stack and relational database"). Status: ratified 2026-09-02.

### D-011 — Phase 3 scope: counterfactual engine first, computed on demand and never persisted; browser replay experience is the next milestone

- **Decision:** Phase 3 is split into 3A and 3B. **3A** builds the counterfactual engine and its proofs only: a `replay` path that re-verifies and reproduces the original decision, reads the same sealed historical context through the same code path, evaluates it under an explicitly selected and verified current artifact, and returns a result typed and labeled as a counterfactual; a structured comparison exposing both logic identities, scores, thresholds, outputs, contribution changes (changed, added, removed, reweighted) with evidence references, and every input current logic expects that the historical context lacks, with `unavailable` and `absent` distinct; and automated tests for AC-02, AC-06, the counterfactual halves of AC-03 and AC-04, AC-07 for the current artifact, and INV-06. Counterfactual results are computed on demand and are **not persisted**: no table, column, event, or cache holds one. 3A's completion is "counterfactual engine complete", not "replay experience complete" or "MVP proven". **3B**, the next milestone and required product work, is the smallest browser experience in which a viewer inspects the original decision and its historical evidence, runs replay, and sees the contribution differences and the counterfactual label: one decision-detail screen with a replay panel (§4.4 and §4.5 together) is acceptable. Missing-input coverage in 3A uses a distinct test-only artifact with its own identity; canonical `v5.1` is unchanged and is not claimed to require `website_intent`.
- **Why:** The 86 → 51 comparison is the central replay moment, and its correctness (no present-day evidence entering, original never blurred with the counterfactual) must be proven at the engine level before a screen displays it; a screen built first would be reviewed for layout while the semantics were still unproven. Persisting counterfactuals is not required by any acceptance criterion, would add a protected table and a persistence story to INV-06 for no proof value, and can be added later by a further decision if a real need appears. Naming 3B now as required work keeps demo readiness on the plan rather than behind unspecified backend work.
- **What stays true:** The complete MVP still requires the collector-backed canonical trace, action, cost and outcome context, explicit attribution, and event-derived aggregate Insights; the build-complete and portfolio-ready gates (including the 30-second understanding and the 60–90 second demo) are unchanged; RelayBridge and NovaSignal AI remain the fictional canonical roles; canonical results (`v3.2` → 86 / `PRIORITIZE`, `v5.1` → 51 / `DO_NOT_PRIORITIZE`, threshold 75) are preserved; no rule editor, no additional replayable decision class, no live vendor integration.
- **Provenance:** `02` §4.5, §5 ("Three things stay distinct", "Replay honesty"), §9 AC-02, AC-03, AC-06, §10 INV-03, INV-06, INV-09, §13 principles 1, 5, 9, 10; user's decision packet 2026-09-08 choosing Option A among the three scoping options put by the coordinating Claude session (A: engine and proofs; B: A plus the comparison page; C: persisted counterfactuals). Status: ratified by user decision 2026-09-08.

### D-012 — Phase 3B presents recorded outcomes without attribution; the complete `outcome-attribution-v1` engine is Phase 4

- **Decision:** Phase 3B stays presentation-only. The decision-detail screen renders recorded outcome observations with their available type, time, and evaluation-window information, and shows `Attribution not yet evaluated` where `PRODUCT.md` §4.4 requires an attribution status. That wording is a presentation state meaning no attribution evaluation exists yet: it is not a fourth `outcome-attribution-v1` result, it is never substituted for `unresolved`, and it is never persisted. Attribution means policy-based linkage under D-007, not proof of causation. Until attribution exists, outcome observations are labeled at their actual scope (recorded account outcomes); account membership alone MUST NOT imply that an outcome belongs to a particular decision, and a source-provided action or decision reference is displayed as a recorded reference without any claim that it has been validated. Missing outcome data, an explicitly recorded open-window state, and recorded negative outcomes stay distinct, and no outcome-evaluation logic is added to manufacture any of those states. Phase 4 implements the complete D-007 policy and replaces the placeholder with policy-backed `direct`, `inferred`, or `unresolved` results carrying policy version, method, window, references, and attribution time, with inferred attribution labeled heuristic. The placeholder does not satisfy §4.4's attribution-status requirement, AC-16, or the attribution and open-window clauses of INV-08; those remain open.
- **Why:** This is a sequencing decision, not a reduction of the MVP requirement. It delivers the inspectable decision and replay screen that D-011 names as required product work while preserving one coherent attribution implementation. Implementing only the direct-reference case now would introduce partial policy semantics into a phase scoped as presentation over proven data, and could label an inferable outcome unresolved.
- **What stays true:** D-007's complete policy, historical-context sealing, immutable logic identity, canonical scoring behavior, and the separation of original and counterfactual results are unchanged. Actual outcomes are never attributed to counterfactual decisions or actions.
- **Provenance:** `02` §4.4, §5 ("Outcomes are later, separate observations", "Outcome attribution policy"), §9 AC-16, §10 INV-08, INV-09, INV-10; D-007, D-011. Status: ratified by user decision packet 2026-09-09.

### D-013 — Phase 4 scope: attribution is a persisted record written through the collector, outcome schema v2 carries source references and window state, delivery splits into 4A and 4B

- **Decision:**
  - **Persistence (Q1).** An attribution result is a durable record, not a view. It enters through the collector as a new event type (`outcome.attributed`) and is written to a new append-only projection. The record carries its own identity, the outcome identity and version it evaluates, the policy version and method, the applied attribution window, the resolved action and decision references when resolved, the attribution time, the status `direct` / `inferred` / `unresolved`, and an inspectable reason for any unresolved or fallback result. The envelope's `account_ref` is the outcome's own account, with an explicit internal attribution source, so an account's attribution appears in that account's trace; `_system` stays a registry sentinel excluded from account surfaces and is never treated as an authentication principal. The collector verifies that a submitted result agrees with the named policy recomputed over a reproducible ledger snapshot; resolving foreign keys alone would let an arbitrary submitted `direct` claim become authoritative, so policy evaluation is shared code between the computation path and the validation path. Each result records a collector-owned ingestion cutoff (or equivalent stable snapshot identity): attribution reads actions recorded by that cutoff, their event times, and the exact outcome version, so a later-arriving action with an earlier occurrence time can never silently change an existing attribution, though it may justify an explicitly appended replacement evaluated against a newer cutoff. Revision uses explicit supersession links; prior rows are retained; cross-outcome links and ambiguous branching are rejected; exactly one effective result is exposed per outcome version and policy. A retry of the same operation reuses its identity and timestamp and has one domain effect, and a new event id must not create a second effective result for the same operation. Results are computed and submitted by a small explicit command or service invocation; browser reads stay read-only; no scheduler, queue, or background platform is introduced.
  - **Outcome schema v2 (Q2).** Schema v1 validation and canonical serialization are unchanged, and version-aware handling is added for the outcome payload. v2 carries an optional source action reference and an optional source decision reference, including neither, and preserves those source values separately from policy-resolved references: a supplied source reference is a claim to evaluate, never proof of credit. Under v2 a well-formed but unusable source reference is retained with a reason and D-007's fallback then applies; unknown, cross-account, ineligible, or mutually inconsistent references never yield direct credit; structurally malformed envelopes are still rejected atomically; v1 keeps its existing stricter reference rejection. Raw source references therefore cannot carry the foreign-key constraints that validated attribution targets carry. The decision-only direct-reference case is supported: a valid original prioritization decision reference resolves directly without inventing an action reference, and when both references are supplied their recorded relationship must agree. An explicit action qualifies only as an eligible actual action under `PRODUCT.md` §5. Eligibility is pinned to normalized instants: strictly before the outcome, no more than 90 days earlier, same account, linked to an original prioritization decision, with sent or completed outbound status; ties on equal action occurrence times break deterministically by collector ingest sequence. The policy's attribution lookback is distinct from the outcome's evaluation period.
  - **Window state (Q3).** v2 records an explicit evaluation state, period bounds, and an observation or as-of time, independent of attribution status: an open observation may still carry direct or heuristic linkage, and a closed observation may be unresolved. A false observation in an open window means no observation recorded as of the stated time, never final failure; supplied unknown values are preserved and no false defaults are manufactured. A negative evaluated outcome stays distinguishable from an open observation and from no outcome data, and advancing the browser's clock never creates an evaluation or turns missing data into a negative result. Closing or correcting an observation appends a new version linked to the version it replaces, the earlier observation and its attribution are retained, and the effective outcome-selection path never counts both. v2 validates period ordering and state/as-of consistency. v1 remains the recorded evaluated representation, including the unchanged canonical outcome.
  - **Delivery (Q4, Q5).** Phase 4 extends the existing decision screen only: the outcome section shows the persisted result's status, policy, method, attribution window, resolved action and decision references where present, attribution time, and an explicit heuristic label on `inferred`, distinguishing source-provided from policy-resolved references and whether the credited decision is the one being viewed. Outcomes stay account-scoped, including unresolved ones. `Attribution not yet evaluated` remains for an outcome with no result, distinct from `unresolved` and from an evaluation failure. Superseded results are inspectable within the existing trace and detail surfaces. No separate attribution dashboard or aggregate screen. The phase splits: **4A** = versioned outcome schema, append and supersession representation, the deterministic policy, persisted collector-backed results, canonical attribution generation, and the automated semantic proofs, completing as "attribution engine and persistence complete"; **4B** = the decision screen's outcome and attribution states with rendered-page and visual checks, completing as "attribution experience complete, subject to explicit acceptance and invariant mapping". 4A must not claim all of AC-16 closed while the decision-metrics integration is unimplemented: 4A proves the effective resolved-outcome selection boundary, and the end-to-end metrics exclusion closes through Insights in Phase 5.
- **Why:** `PRODUCT.md` §6 names Outcome Attribution as a record with a stable identifier and an attribution time, and INV-08 gives it append and supersession semantics, so D-011's ephemeral treatment of counterfactuals does not transfer to it. A versioned outcome payload preserves the identity of every already-accepted event while adding the reference and window states D-007 and INV-08 require. Engine and persistence before presentation keeps each review tractable, as it did in Phase 3.
- **Corrected premises (recorded so they are not relitigated):** the coordinating session's Phase 4 packet claimed that every outcome schema v1 accepts must attribute `direct`. That is wrong. `ActionRecordedPayload.status` admits `failed`, and the outcome's reference validator checks only that the action exists, belongs to the same account, and occurred strictly before the outcome; it checks neither execution status nor the 90-day bound. Outcomes referencing a failed or long-past action are accepted today, so `inferred` and `unresolved` are not categorically impossible under v1. The real v1 gap is that an ordinary reference-free observation and an explicit decision-only reference cannot be expressed. The packet also understated the identity risk: the collector hashes the validated model's JSON dump (`envelope.model_dump(mode="json")`), not the submitted bytes, so adding new defaulted fields to the outcome model can change the canonical hash of payloads whose submitted bytes are identical. That is why Q2 is a new schema version rather than an optional field added to v1, and why Q2 and Q3 are decided together.
- **What stays true:** canonical scores 86 and 51, threshold 75, outputs, rules and evaluator identity; sealed historical context; original and counterfactual separation and the rule that no counterfactual is persisted (D-011); synthetic labeling; the single collector boundary; the build-complete and portfolio-ready gates. Existing canonical envelopes stay byte-for-byte unchanged and attribution is added through the collector. No new consequential dependency.
- **Relationship to D-012:** D-012 deferred policy-backed attribution and open-window presentation to Phase 4 and is superseded only in that sequencing respect. Its presentation state survives: an outcome with no attribution result still reads `Attribution not yet evaluated`, which is not a policy result.
- **Provenance:** `02` §4.4, §5 ("Outcomes are later, separate observations", "Outcome attribution policy"), §6 (Outcome Attribution), §9 AC-10, AC-15, AC-16, §10 INV-01, INV-02, INV-06, INV-08, INV-09, INV-10, INV-11; D-007, D-009, D-010, D-011, D-012. Reviewed by the ChatGPT project reviewer at repository commit `8303dbc9a2a076362b43530b29e23d3e2be1e67e` (ACCEPT WITH CONDITIONS; the conditions are implementation requirements, recorded above). Status: ratified by the user on 2026-09-10, stating "i ratify the decisions".

### D-014 — Phase 5 scope: Insights as a read-only metric contract over effective selections, the seeded 200-account dataset as a bounded operation schedule through the collector, the portfolio minimum, no deployment, delivery in 5A / 5B / 5C

- **Decision:**
  - **Insights metric contract (Q1).** Insights is computed at one display cutoff `c` captured once per read (`ledger_maximum`), which bounds the decision population (`decisions.ingest_sequence <= c`, `account_prioritization` only, accounts through `accounts_query()`), the outcome versions and the attribution results alike. Effective observations are `effective_outcome_versions(cutoff=c)` and, for each, `effective_attribution(..., cutoff=c)`; superseded outcome versions and superseded attribution rows never count anywhere; `AmbiguousSelection` fails the affected calculation visibly with the version named and no partial rate. Each effective observation has one attribution standing (`awaiting attribution`, `unresolved`, `direct`, `inferred` labeled heuristic), one observation state (`open` = v2 open; `closed known` = v1, or v2 closed with a known `opportunity`; `closed unknown` = v2 closed with `opportunity` null) and a recorded period (v1 `window_days`; v2 `window_closes_at − window_opened_at` on normalized UTC instants). An observation is attributed to decision `d` only when its effective attribution is `direct` or `inferred` with `resolved_decision_event_id == d`; an unresolved result has no resolved decision and therefore never enters any decision-level metric (the AC-16 exclusion); a successor version awaiting attribution inherits no credit. An open observation is never completed whatever its values (an open `false` is not a negative, an open `true` is not a positive); a closed unknown is never a `false`; neither state nor period is ever inferred from the attribution lookback or from a clock. A *qualifying 90-day observation* is attributed, `closed known`, and has a recorded period of exactly 90 days (v1 `window_days == 90`; v2 exactly 90 × 24 hours); other periods stay visible in coverage as `other evaluation period` and never enter the 90-day rate; the collector's admission of other periods is unchanged. Reconstruction status is orthogonal: a decision whose `reconstruct` raises stays in the population and every coverage and standing count, is listed with its reason, is excluded from every matched count and every rate, and is never `known false` for any signal. Decision observation standing, one per decision by precedence over its attributed effective observations of every period: `evaluated` (any closed known), else `unknown` (any closed unknown), else `open` (any attributed observation), else `unattributed`; the four standings partition the population and outcome-level state counts are shown beside them. For a decision set `S`, `eligible(S)` = reconstructed decisions in `S` holding at least one qualifying 90-day observation; the 90-day opportunity rate = decisions in `eligible(S)` with at least one qualifying observation whose `opportunity` is true, over `|eligible(S)|`, each decision counting at most once, meaning "at least one recorded positive observation in a completed 90-day evaluation window" and never a conversion probability; two independent qualifying chains one true and one false yield one positive decision; a true superseded by a false within one chain yields only the effective false; `eligible` and `evaluated` may differ and both are shown with exclusion counts by reason, never as a partition; a zero denominator renders `not available (0 eligible decisions)`, never `0%`, and a comparison with an empty arm has no percentage-point result; every rate shows numerator, denominator, cohort total and window. Signals are named by the planted-effects manifest as an input key plus either a *rule* predicate (a factor key and exact rule text of the decision's own historical artifact; `recently_funded` is `funding_event` with `funding_event observed within 90 days before the decision boundary`) or a *context-value* predicate on the value preserved in sealed `H(d)`, read through the stored `evidence_version_id` only, regardless of consumption (`verified_integration_pressure = HIGH`). Per decision a signal has exactly one state: `known true`, `known false`, `unavailable`, `absent`, `not applicable` (rule signals whose artifact lacks that exact factor), or `reconstruction failed`; only `known true` and `known false` are comparison arms and every other state is a visible excluded count. The signal table shows *comparison signal present* (known true) separately from *historical rule matched* (matches of the decision's own artifact rule on that key, named by its text, or `not applicable`), plus input available and input consumed; under `v3.2` HIGH pressure is signal present, available, ignored, rule not applicable, and under `v5.1` signal present, available, consumed, LOW rule unmatched; no HIGH factor exists or is invented. Matching comes from exact reconstruction (`FactorResult.matched`), never from the sign of a stored contribution (Q2). Workflow rows compare `v4.2` against the manifest's comparison cohort on the same rate with both arms' eligible denominators. Coverage counts derive from the effective selections, never from row counts over `outcome_attributions` or `outcomes`. Every rate carries "observed" and its `n`; the page states that the data is synthetic, the comparisons descriptive, counterfactual results never recorded and in no number shown, and that no simulation view exists in the MVP; planted effects are labeled demonstration checks.
  - **Reads (Q3).** Pure read-only functions in `analytics/` taking `(conn, cutoff)`; an `/insights` route captures the cutoff once and renders it; nothing is persisted; no analytics table; no new dependency.
  - **Dataset (Q4).** A seeded code generator (standard-library `random`, config and planted-effects manifest under `fixtures/dataset/`) whose envelopes enter through the collector. The seed is a deterministic, bounded operation schedule derived only from the config: the canonical nine first and unchanged (the generator never emits `novasignal-ai` and never registers an artifact; its decisions reference the canonical artifact hashes loaded from `fixtures/canonical/`), then stage-1 generated envelopes in a dependency-respecting order (occurrence order where dependencies allow, ties by a stable generated sequence key; no global chronology promised; `occurred_at` and `recorded_at` distinct), then one attribution operation per stage-1 effective outcome version at the scheduled stage-1 cutoff `c1 = 9 + N1` with a fixed configured instant `A`, rebuilt identically on every run with `attribute`, `build_envelope` and `submit` so the collector answers created once and duplicate thereafter, then stage-2 envelopes (one correction, one new observation) that have no attribution operation and remain awaiting attribution in the demo. General attribution never runs inside the seed; the standalone `attribute` command keeps its general behavior and may legitimately attribute the stage-2 outcomes later. Before its first operation the seed checks the ledger prefix and any stored operation against the schedule and stops visibly, writing nothing, on any divergence or on any collector answer other than created or duplicate; it never retargets to a newer maximum, never supersedes, deletes or reverses an independent result, and never claims a fresh-seed digest for such a ledger; only `reset` returns a ledger to fresh. No tracking table, marker file, scheduler, mutable seed state or new write-admission rule. Generated outcomes use schema v2; the canonical outcome stays v1; the demo holds all four attribution standings, all three observation states, at least one correction history, at least one non-90-day period and `unresolved` cases. Artifact selection reads activation instants from the canonical artifacts (`v3.2` from `2026-01-12T09:00:00Z` before `2026-06-01T09:00:00Z`, `v5.1` from that instant) with no duplicated constants; a fixed horizon `H` bounds every `observed_at`; windows open at the action's `occurred_at` and close at opened plus the configured period; closed observations are stamped at window close; `A >= H`. Determinism is an ordered logical digest over `(event_id, canonical_hash)` in ingest order including attribution events plus the aggregates, pinned literally, never file bytes; two fresh seeds match; a completed-seed retry changes no row, digest, aggregate or awaiting-attribution identity; interrupted-stage recovery at every stage boundary and mid-stage yields the fresh-seed ledger. Thresholds live only in the manifest; an independent test asserts the manifest honors `PRODUCT.md` §8 (at least 200 prospect accounts; populations at least 50, 40, 40; the 2 / 10 / 8 percentage-point bounds in their directions); effects are recovered from ingested events on unrounded rates with non-empty arms and NovaSignal AI participating wherever eligible; hand-calculated cases and controlled mutations prove the numbers are computed, not read from the manifest.
  - **Portfolio (Q5).** README (problem, thesis, architecture, replay semantics, synthetic boundary, setup, tradeoffs; the Merge sentence; the `invariants` CI badge; a Mermaid architecture diagram; the collector example inline; the demo path as `PRODUCT.md` §7's eight steps with URLs), PNG screenshots under `docs/screenshots/` (trace, decision with replay 86 → 51, Insights) captioned synthetic and taken at a stated commit, recording optional and outside the repository; acceptance requires an executed fresh local setup from the README on a clean checkout and an executed browser walk of the demo path. Nothing beyond that minimum.
  - **Hosting (Q6).** No deployment work in Phase 5; flight-recorder.app stays parked; a read-only public demo remains a later candidate requiring its own decision packet and is a condition of neither completion gate; no provider, domain configuration, write guard or hosting dependency enters this scope.
  - **Delivery (Q7).** **5A** = generator, config, manifest, the dataset seed mode with its schedule, the `analytics/` functions and the proofs (AC-13, AC-14, AC-16's last clause, AC-11, INV-03, INV-10, and the reviewer's required-evidence boundaries: AC-16 / INV-08 / INV-10, effective selection, cutoff isolation, AC-11 / INV-03 / INV-09, AC-13 / AC-14 / INV-11, INV-01 / INV-02 / INV-04 / INV-06), reusing the existing selector and policy code, completing as "dataset and analytics engine complete". **5B** = the `/insights` page, NovaSignal AI findability on the account list, rendered-page, visual and keyboard evidence under the existing keyboard ruling, completing as "Insights complete; AC-16 closed" only with the engine-to-page exclusion mapped to the accepted commit. **5C** = README, screenshots, diagram, badge, demo path, and the assessment of AC-01 through AC-18 and both `PRODUCT.md` §12 gates against delivered evidence, recorded in `STATE.md` with the checklist inside the review handoff.
- **Why:** The metric contract is the smallest set that satisfies §4.6, §7 step 5, AC-11, AC-14 and AC-16 while keeping every distinction the 4A review fixed (effective versions before attribution; open / evaluated / unknown / unresolved / unattributed; open `false` never a negative; unknown never a `false`; row counts insufficient) visible as a number. A read-only view at one cutoff matches the decision page and D-011's precedent, and no acceptance criterion names an analytics record. A bounded operation schedule is the only seed shape that is both deterministic and safely repeatable next to the existing general attribution command, which attributes every unattributed effective version at the current maximum. The portfolio minimum and the hosting deferral follow `PRODUCT.md` §12 and §13 principle 10.
- **What stays true:** the collector contract and every canonical envelope byte for byte; the nine pinned hashes; the evaluator and rule grammar; sealed historical context; immutable logic identity; original and counterfactual separation and non-persistence (D-011); D-013's attribution, window and supersession semantics and the 4A rulings; canonical scoring 86 / 75 / `PRIORITIZE` and 51 / `DO_NOT_PRIORITIZE`; RelayBridge and NovaSignal AI as the only named roles; append-only projections; no new consequential dependency. The stale-outcome admission proposal stays unratified and outside Phase 5.
- **Provenance:** `02` §4.2, §4.6, §7, §8, §9 AC-11 to AC-14, AC-16, AC-19, §10 INV-03, INV-06, INV-08 to INV-11, §11, §12, §13; D-006, D-007, D-008, D-010, D-011, D-013. Decision packet `.handoffs/decision-packet-phase-5.md` revision 3 (SHA-256 `44db7e41b40c9c12c023ea9cf48a28bb38ec447f36c9e8d1b537db787ae4263f`), reviewed by the ChatGPT project reviewer at repository commit `3e96c1916b2edbab5a4804cfaa22275f0aa45b5a` in two rounds (`.handoffs/review-disposition-phase-5.md`, five findings; `.handoffs/review-disposition-phase-5-r2.md`, one finding), each incorporated; the reviewer stated that direct ratification of the corrected recommendation needs no further packet review. Status: ratified by the user on 2026-09-14, stating "ratified".

### D-015 — Phase 5 dataset: observations for decisions with no recorded action open their window at the preserved decision boundary (clarifies D-014 Q4)

- **Decision:** For the Phase 5 synthetic dataset, action-backed observations keep D-014's rule (the evaluation window opens at the action's `occurred_at`). Observations are additionally permitted for decisions with no linked action, of either decision output and under either canonical artifact, with their evaluation window opening at the preserved decision boundary and `observed_at` strictly after it. Such an observation may carry a valid source decision reference and is attributed by the unchanged `outcome-attribution-v1` policy (a valid decision-only reference resolves `direct` with no resolved action, per D-013 Q2; a reference-free observation follows the ordinary account-wide fallback). Recorded periods, states, observation times and rate eligibility follow D-014's metric contract unchanged. A missing observation stays missing; it never becomes a generated negative merely because there was no action. The dataset describes such an observation as recorded for a decision with no action recorded, never as evidence of no outreach at account scope, and never synthesizes an action for it.
- **Why:** Under the canonical weights `v3.2` outputs `PRIORITIZE` only when the funding factor matches (positive weights 86, 68 without it, threshold 75) and `v5.1` cannot reach its threshold at all (positive weights 72), so every decision without recent funding is `DO_NOT_PRIORITIZE` and, with outcomes recorded only after actions, the `recently_funded` comparison of `PRODUCT.md` §8 had no eligible negative arm. This clarification supplies that arm from existing collector and attribution capability, preserves truthful action history and every canonical identity, and lets recorded `v5.1` decisions have observed outcomes despite their output. Canonical scoring, the attribution policy, the planted effect definitions and the metric definitions are not changed to solve the gap.
- **What stays true:** D-014 in full apart from this window-origin clarification; D-007 and D-013; the canonical weights and the retained finding that `v5.1` cannot prioritize any context (a separate user decision, outside Phase 5); no new dependency, persistence, page or hosting work.
- **Provenance:** Builder stop-and-report on task P5-A revision 2 at `c61e163` (nothing written); coordinator verification from the fixture weights; delta packet `.handoffs/review-packet-p5a-delta.md`; ChatGPT project reviewer disposition `.handoffs/review-disposition-p5a-delta.md` at `57d6a73e484bff4969c5cbaf7abde14cc31f13b1` (ACCEPT WITH CONDITIONS; this is its condition 1, recommended in its section 1). Status: ratified by the user on 2026-09-14, stating "ratification accepted".

### D-016 — Stale-outcome write admission: a new attribution write must name the outcome version that is effective at submission time, checked at the current ledger maximum inside the validation-and-write transaction

- **Decision:**
  - **The admission rule.** Every new `outcome.attributed` write must name the currently effective version of its outcome's supersession chain, resolved at the **current ledger maximum** at submission time, not at the payload's `ingest_cutoff`. The check runs inside the same transaction as the rest of validation and the write, so the answer cannot drift between the check and the append. A submission naming a superseded version is refused atomically by a named rejection reason whose detail identifies the effective version, so a client knows what to evaluate instead. Nothing is written on refusal.
  - **No silent repair.** If an outcome correction lands between the moment a result is computed and the moment it is submitted, the submission is rejected. The collector never retargets the outcome, never re-freezes the cutoff, and never recomputes on the client's behalf. A later explicit command invocation evaluates the effective version.
  - **Conflicting retries are unchanged and are narrower than they might sound.** A conflicting retry means the **same `event_id` carrying different content**, which keeps its existing 409 with reason `event_id_reused_with_different_content`. It does not mean every different attribution for the same outcome. A valid replacement attribution remains permitted exactly as before, under D-013's supersession semantics and the strictly newer `ingest_cutoff` rule (`replacement_attribution_cutoff_is_not_newer`); this decision changes nothing about attribution supersession, only about which **outcome version** a new write may name.
  - **New writes only.** This is not a prohibition on computing, inspecting or reproducing attribution for historical versions. `policy.attribute` and both selection helpers keep working over any version at any valid cutoff; every already-stored row stays readable and reproducible at its own recorded cutoff; an exact submission retry still answers 200 duplicate with one domain effect even after the outcome it names has been superseded. No schema migration, no new dependency, and no rewrite of historical data.
  - **Boundary, not client.** The rule is enforced at the collector boundary, because the collector is the only door (INV-11). Fixing only the `attribute` command would leave the hole open to any direct submission.
  - **Dispatch.** This is a bounded correction against `main` at `4b3ec231ae6239be41cf8c7891eeb010d6760875`, delivered as its own task file with a pre-dispatch review, not by reopening the closed gate in `.handoffs/phase-4-task-4a-02.md`, whose base and branch are obsolete. This entry authorizes the rule and the preparation of that task. It authorizes no merge and no deployment.
- **Why:** The read path already excludes an obsolete attribution from every selection, so the reproduced case is not evidence of corrupted current metrics. Accepting a fresh result for an obsolete observation is nevertheless avoidable and misleading write behavior: the ledger would carry a record that looks current and is not. The same check already exists for `outcome.evaluated`'s own supersession link (`superseded_outcome_is_not_effective`), so the rule makes the collector consistent with itself rather than adding a new kind of guard. Checking at the ledger maximum rather than the payload's cutoff is deliberate: the question is the state of the ledger when the write arrives, not the snapshot the result was computed over.
- **Known consequence, recorded so it is not mistaken for a defect:** `tests/acceptance/test_insights_engine.py::test_a_late_attribution_to_a_superseded_outcome_is_excluded` submits exactly this write through the public collector today and asserts 201. Under this rule that submission is refused, so the test must be reworked to keep its read-side proof (historically stored stale results stay excluded from every selection) without requiring a newly forbidden public write. The read-side coverage is preserved, not deleted. This is the only test found in the suite that posts an attribution naming a knowingly superseded outcome; the remaining attribution tests exercise attribution supersession, which is untouched.
- **What stays true:** D-013 in full, including attribution persistence, outcome schema v2, supersession semantics and the 4A rulings; D-014 and D-015, including the metric contract, the seed schedule and the hosting deferral; the collector contract and every canonical envelope byte for byte; the nine pinned hashes; canonical scoring 86 / 75 / `PRIORITIZE` and 51 / `DO_NOT_PRIORITIZE`; cutoff monotonicity; exact-retry duplicate and event-id conflict semantics; append-only projections; INV-01's immutability of stored records; the `v5.1` fixture-weight finding, which stays a separate open question outside this decision.
- **Relationship to the earlier proposal:** Section 3 of `.handoffs/phase-4-task-4a-02.md` specified this rule behind a ratification gate at its section 0 that was never opened, and `STATE.md` has carried it since as an unratified proposal and the user's decision. That gate is now moot: the rule is ratified here, and the historical file stays `.handoffs/` evidence rather than a dispatch.
- **Provenance:** `02` §9 AC-15, AC-16, §10 INV-01, INV-08, INV-10, INV-11; D-013 (Q1 attribution persistence and supersession), D-014, D-015. Reviewer assessment `.handoffs/post-merge-findings-2026-09-19.md`, section "1. Resolve stale-outcome write admission first", written against comparison base and reviewed HEAD `4b3ec231ae6239be41cf8c7891eeb010d6760875`, where the ordering gap was reproduced against a disposable database (HTTP 201, one event appended, although the effective outcome is the replacement); historical specification in section 3 of `.handoffs/phase-4-task-4a-02.md`. Status: ratified by the user on 2026-09-19, stating "I ratify the recommended stale-outcome admission rule as D-016, with this correction: conflicting retries mean the same event ID with different content, not every different attribution for the same outcome. Valid replacement attributions remain permitted under the existing supersession and strictly newer-cutoff rules. Check outcome effectiveness at submission time inside the validation-and-write transaction. Preserve exact retries, historical records and reproducibility, and reject stale submissions without silently redirecting or recalculating them. Record D-016 and prepare the bounded builder task against current main for review. This authorizes that rule and task preparation, not merge or deployment."

### D-017 — Scoring correction by immutable successor: register `v5.2`, whose maximum attainable score clears its threshold, and make it the demo default without editing or retiring `v3.2` or `v5.1`

- **Decision:**
  - **The defect.** The canonical `v5.1` artifact's positive factors sum to 72 against a threshold of 75, so no historical context can reach its threshold and the ratified "current logic" can only ever output `DO_NOT_PRIORITIZE`. Confirmed over the shipped synthetic dataset: 0 of its 240 generated accounts reach `PRIORITIZE` under `v5.1`. This is a scoring-design defect, not an artifact-integrity defect.
  - **The correction is a successor, never an edit.** A new immutable artifact `logic-account-prioritization-v5.2`, `logic_version` `v5.2`, is registered. `v3.2` and `v5.1` keep their exact content, their canonical content hashes (`db3a8bde…82db0` and `b5a6f33d…7a3b`), their activation fields and their registration events, byte for byte. No stored decision, outcome, attribution or projection row is rewritten, re-scored or migrated.
  - **The single factor change.** `v5.2` carries `v5.1`'s five positive factors unchanged — `employee_count` +25, `industry` +20, `funding_event` +4, `open_platform_engineering_roles` +15, `headquarters_country` +8 — and replaces the sixth. `verified_integration_pressure equals 'LOW'` at weight −21 becomes `verified_integration_pressure equals 'HIGH'` at weight +15. Threshold stays 75. `missing_value_behavior`, output mapping, artifact schema version and `evaluator-v1` are unchanged. A second factor on the same key is not permitted (the strict model requires unique factor keys), so the pressure signal is expressed once, as a positive.
  - **What the rule set then means.** Its positive weights total 87 against threshold 75, and 87 is attainable. `employee_count`, `industry`, `open_platform_engineering_roles` and a verified `HIGH` integration pressure are jointly necessary and, at exactly 75, jointly sufficient; `funding_event` and `headquarters_country` are bonuses, so a context missing one of them can still reach `PRIORITIZE` at 83 or 79. Missing **integration-pressure** evidence stays conservative: verified `LOW`, an absent input and an input recorded unavailable all contribute 0 and all score 72 on the canonical context, so no context lacking verified `HIGH` pressure can reach the threshold, while the three input states stay separately visible per INV-03.
  - **Demo default, not activation truth.** `v5.2` becomes the demo's default replay logic, resolved as today by unique label match and then used by hash. Registering a successor does not retire its predecessor: `v5.1`'s stored artifact keeps `status` `current` and a null `deactivated_at` because it is immutable and hash-pinned, and nothing in the engine reads those fields to choose an artifact. The selected demo default and an artifact's recorded activation metadata are distinct things and are described as distinct. Any registered artifact stays selectable by hash, including `v5.1` at `b5a6f33d…7a3b`.
  - **Canonical comparison.** The original 86 → 51 comparison remains exactly reproducible whenever `v3.2` and `v5.1` are the selected pair. The default rendered comparison on the canonical decision becomes 86 → 72, still `PRIORITIZE` → `DO_NOT_PRIORITIZE`, because NovaSignal AI's verified integration pressure is `LOW` and that factor contributes nothing.
  - **A small visible warning.** Where an artifact is presented, the application states plainly when the sum of that artifact's positive weights is below its threshold, naming both numbers. That sum is an **upper bound** on any attainable score under `evaluator-v1`, not a proven maximum: the closed grammar admits a rule no context can satisfy, so a positive weight can inflate the bound without being reachable. The warning therefore reports the bound and its consequence, and its absence is never evidence that a threshold is reachable. It is a read-side property of preserved artifact content. It rejects nothing, changes no stored content, blocks no registration and alters no score; `v5.1` keeps rendering, replaying and reconstructing exactly as it does now, with the warning shown beside it, and existing artifact-integrity and unsupported-rule failures keep their present visible treatment.
  - **Registration is repeatable and collector-only.** `v5.2` is registered through the public collector as an overlay that lives outside `fixtures/canonical/`, so the nine canonical envelopes, their nine pinned hashes, the two-artifact generator pair and the dataset seed schedule are untouched. Re-running the registration is a duplicate with no second domain effect. The overlay is registered **after** the dataset is complete: the seed schedule requires every event inside its scheduled prefix to occupy its exact sequence, so an overlay registered first displaces a scheduled event and the next `seed-dataset` refuses with `ScheduleDiverged`. The added registry event advances the ledger cutoff and adds no decision, action or outcome; a ledger carrying the overlay is reported as not a fresh seed and is not claimed to hold the fresh-seed digest or event count.
  - **Weights are a demonstration choice.** These weights are synthetic demonstration choices chosen to make both outputs reachable and the factor set legible. They are not business rules, and they are not validated by the synthetic Insights results, which describe planted effects in generated data. Product wording says so.
  - **Dispatch.** This entry authorizes the rule, the successor artifact and the preparation of a bounded builder task against `main` at `6b6da168fb0fdc9ffce8ef1d3a2764c03757429e`, with a pre-dispatch review. It authorizes no merge and no deployment.
- **Why:** Editing `v5.1` in place is forbidden by D-005, INV-05 and AC-17 — changing any weight or threshold creates a new identity — and would break the exact replay the product exists to demonstrate. Lowering `v5.1`'s threshold in a successor was tested and rejected on the merits: at threshold 65 the artifact scores 72 and outputs `PRIORITIZE` for an account whose integration pressure is absent or unavailable, identically to one with good evidence, which makes absence of evidence a point in the account's favor and drops integration pressure from necessary to optional. Moving the pressure factor from a penalty on bad evidence to a reward for verified good evidence is the smallest change that makes both outputs reachable while keeping missing evidence conservative and keeping the factor the `v3.2` → `v5.1` narrative identifies as decisive. The schema admits a threshold above the sum of positive weights with no complaint, which is why the defect shipped silently; the visible warning surfaces that condition without rejecting the historical artifacts that carry it.
- **Known consequences, recorded so they are not mistaken for defects:** the canonical decision's default counterfactual score changes from 51 to 72 and its score delta from −35 to −14, so `README.md`, the replay screenshot under `docs/screenshots/` and the demo-path narration must be brought into agreement; the documented setup path becomes `reset` → `seed-dataset` → `register-current-logic` → `serve`, and a canonical-only setup (`seed` then registration) cannot afterwards be extended by `seed-dataset` in the same database; without the registration step the default replay logic resolves to nothing and the decision page says so in its existing message; the ledger event count rises by the one registry event, so any recipe line quoting a count or a cutoff is restated rather than left stale; existing tests that assert `v5.1` as the resolved default need scoped maintenance, which is authorized work rather than an unexpected discovery; two registered artifacts carry `status` `current` at once, which is a consequence of immutability and is described, not corrected.
- **What stays true:** D-003, D-004, D-005, D-010, D-011, D-013, D-014, D-015 and D-016 in full; `PRODUCT.md` §7's canonical NovaSignal AI facts and AC-01 and AC-02 (`v3.2` → 86 / `PRIORITIZE`, `v5.1` → 51 / `DO_NOT_PRIORITIZE`), which are statements about those two artifacts and stay exactly reproducible; the nine canonical envelopes byte for byte and their nine pinned hashes; the collector contract, the closed rule grammar, `evaluator-v1` and its temporal semantics; the dataset seed schedule and its digest, the planted-effects manifest and every metric definition; INV-01's immutability of stored records; append-only projections; the hosting deferral of D-014. No schema version, no migration, no new dependency, no new decision class.
- **Provenance:** `PRODUCT.md` §5, §6, §7, §9 (AC-01, AC-02, AC-07, AC-17), §10 (INV-01, INV-03, INV-05); D-005 (immutable declarative logic, new identity on any change); D-015, which first recorded the arithmetic (`v5.1` positive weights 72, threshold 75) and referred it out of Phase 5 as the user's question; D-016, which carried it forward as a separate open question; reviewer assessment `.handoffs/post-merge-findings-2026-09-19.md`, section "2. Correct the scoring design with an immutable successor", which reproduced the ceiling by probe and recommended a separately identified successor rather than a rewrite or a relabel; unratified preparation in `.handoffs/launch-preparation-and-scoring-decision.md`, which proposed this factor change and is superseded as a proposal by this entry while staying `.handoffs/` evidence. Coordinator verification on 2026-09-21 against the shipped evaluator and the shipped 240-account generator, not by hand, each generated account evaluated at its own preserved decision boundary: `v5.1` 0 of 240 generated accounts `PRIORITIZE`; the ratified `v5.2` 23 of 240; canonical context under `v5.2` 72 / `DO_NOT_PRIORITIZE`; the rejected threshold-65 variant 38 of 240 and `PRIORITIZE` on absent and unavailable pressure. Re-verified on 2026-09-21 by a second, independently written probe: twelve claims checked, twelve confirmed, no discrepancy. Status: ratified by the user on 2026-09-21, stating "I approve the proposed v5.2: replace the LOW integration-pressure penalty with HIGH integration pressure worth +15, keeping the other factors and threshold 75 unchanged. Make v5.2 the demo default. Preserve v3.2, v5.1, their identities, and all historical records. The original 86 → 51 comparison must remain reproducible; the new default comparison becomes 86 → 72. Include a small visible warning when an artifact's maximum score is below its threshold. Do not reject historical artifacts or change their contents. Treat these weights as synthetic demonstration choices, not business rules proven by synthetic Insights. Distinguish the selected demo default from historical activation metadata; registering a successor does not itself retire its predecessor. Record the decision and prepare the bounded builder task for review. Cover repeatable collector-based registration, setup, product wording, tests, README, screenshots and the revised demo. Merge and deployment remain unauthorized."

### D-018 — Public demo release: an inspectable, no-login, read-only interactive demo of the existing application on one paid, continuously running Render service, built from a deterministic synthetic snapshot and linked from the Eskora website

- **Decision:**
  - **What is published.** A public, no-login, read-only interactive demo of the existing Python / FastAPI / SQLite application, on one paid, continuously running Render web service. For each release a deterministic synthetic database is built through the existing collector, then served read-only. Visitors can inspect accounts, evidence, decisions and Insights and compute counterfactual replay with the actual engine. The full writable collector stays available for local operation. The previously proposed `flight-recorder.app` address is used after its control and configuration are verified, and the verified demo is linked from the Eskora website. The release includes a concise project explanation, a finished 60–90-second walkthrough, source-code access and verified local-run instructions, and its first-time experience is checked with two or three unfamiliar viewers.
  - **Constraints.** One small service, the current stack, one worker initially, no persistent disk for the reproducible release snapshot. Domain ownership and DNS state are unverified. The earlier approximately $7/month compute figure is a planning estimate, not an approved spending ceiling and not a promise about the whole bill; the actual plan, account, usage charges and resource adequacy are verified before final deployment approval is requested. Releases are deliberate, and each names how to restore the previous working release.
  - **Acceptance.** The browser demo is usable without login; mutating requests and direct database writes cannot change its data; the local collector keeps working; restart and deployment preserve the intended dataset; canonical replay and Insights stay correct; first-use instructions and readable layouts work; the recorded walkthrough matches the release; a fresh checkout runs the full project; and the published website links reach the verified demo, walkthrough and source. Evidence distinguishes tests run, source inspected, coordinator reports and live observations.
  - **Delivery, through the existing build and review process.** (1) A public-demo release candidate: a scoped builder task reviewed before dispatch, built on a short-lived branch, independently checked by the coordinator, reviewed at an exact commit, merged only on the user's word; it exits with an accepted exact candidate and a concrete deployment proposal naming account, service plan, cost estimate, domain, read-only scope, release revision and rollback action. (2) Deployment of that accepted candidate and live verification, after the user approves the concrete deployment. (3) The website connection and hiring presentation, under `/Users/frank/code/eskora-web/AGENTS.md` rather than this repository's builder assignment, plus the first-time viewer check arranged through the user.
  - **Authority.** This entry authorizes recording and scoped preparation, and ordinary reversible preparation inside that scope. It authorizes no purchase, no public deployment, no DNS change, no website publication and no consequential merge. Final deployment approval is reserved for the tested release, the actual hosting account and the actual cost.
- **Why:** A hiring manager can try the working system immediately, while a technical reviewer can inspect its evidence and reproduce it locally. Read-only serving keeps the shared demonstration stable without adding accounts, tenant infrastructure or a managed database.
- **Must not change:** account prioritization stays the only fully replayable decision class. All core invariants, canonical fixtures and identities, evaluator semantics, collector semantics for local use, attribution rules, the seed schedule, and the honest synthetic and counterfactual labels are preserved. On the canonical context, `v3.2` → 86, explicit `v5.1` → 51 and default `v5.2` → 72 are preserved. No scoring redesign, real integration, rule editor, public upload, signup, production multitenancy, database migration or whole-site redesign enters this work. `PRODUCT.md`'s MVP completion gates are unchanged; this release carries its own bounded completion evidence, and AC-19 stays a SHOULD whose accepted gate is not reopened.
- **Relationship to D-014:** D-014 stays the historical Phase 5 decision; its hosting clause (Q6) described Phase 5's scope and is not rewritten. This entry is the "own decision" that clause anticipated, and it makes the bounded public-demo work active after Phase 5 without reopening Phase 5 or its satisfied gates. It resolves the formerly open question "Whether a deployed demo is built".
- **Provenance:** `02` §13 (a deployed demo: MAY, only if it improves accessibility without disproportionate build time); D-011, D-014, D-017. Approved plan `.handoffs/public-demo-release-approved-plan.md`, prepared 2026-09-23 against `main` at `e36c415fd92aa307f379636ae7235dc81aae79a2`; unreviewed starting material `.handoffs/launch-preparation.patch` and `.handoffs/launch-preparation-and-scoring-decision.md`, which are not task authority and not evidence that any candidate passes. Status: approved by the user on 2026-09-23, stating "ok i agree with all of this, how do we keep this plan and avoid drift and map it to be carried out with the proces we've been using", and confirmed the same day: "I have approved the direction. Record it in the existing repository truth and return the first scoped builder task for review, without reopening the general plan."

## Open (Intentionally Unresolved)

These are not decisions. The ratified sources leave them open; nothing here may be assumed by an implementer without a recorded decision. Per the responsibility boundaries, a high-lock-in choice is proposed with tradeoffs and ratified by the user; ordinary reversible choices inside an approved task do not need an entry.

- **Whether any LLM-generated explanation is included at all** (`02` §6.5 permits it at the interpretive edge; not required).
- **Reversible detail choices inside approved tasks, no entry needed:** SQLAlchemy ORM vs Core usage, vanilla JS or HTMX on a specific page, coverage tooling, adding Alembic or a type checker if they earn their keep.
